# -*- coding: utf-8 -*-
"""
Cotizador: al final del PDF sale el telefono del usuario que la creo y el
de su sucursal (interna de la app), y se agrega "Meses sin intereses" antes
de Notas (a 6 meses = costo integro sin descuentos; cada mes arriba de 6
suma 1% de recargo).

Uso: colocalo en la carpeta del repo (junto a backend/ y frontend/) y corre:
    py fix_contacto_msi.py
"""
import sys

ARCHIVOS = {'backend/db.py': [["        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS tipo_cliente TEXT NOT NULL DEFAULT 'publico';\n", "        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS tipo_cliente TEXT NOT NULL DEFAULT 'publico';\n        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS meses_msi INTEGER;\n"], ['def _enriquecer_cotizacion(cur, cotizacion):\n    cur.execute("""\n        SELECT id, articulo_id, clave, nombre, cantidad, precio_unitario, descuento_pct, nota\n        FROM cotizacion_items WHERE cotizacion_id = %s ORDER BY orden\n    """, (cotizacion["id"],))\n    cotizacion["items"] = [dict(r) for r in cur.fetchall()]\n    cotizacion["total"] = sum(_subtotal_item(i) for i in cotizacion["items"])\n    return cotizacion', 'def _enriquecer_cotizacion(cur, cotizacion):\n    cur.execute("""\n        SELECT id, articulo_id, clave, nombre, cantidad, precio_unitario, descuento_pct, nota\n        FROM cotizacion_items WHERE cotizacion_id = %s ORDER BY orden\n    """, (cotizacion["id"],))\n    cotizacion["items"] = [dict(r) for r in cur.fetchall()]\n    cotizacion["total"] = sum(_subtotal_item(i) for i in cotizacion["items"])\n    # Para el pie del PDF: el/los teléfono(s) del usuario que la creó, y los\n    # de la sucursal (interna de la app, sucursales_reparacion — no la de\n    # Microsip) donde está dado de alta ese usuario.\n    cotizacion["creador_sucursal_nombre"] = None\n    cotizacion["creador_sucursal_telefonos"] = None\n    if cotizacion.get("creador_sucursal_id"):\n        cur.execute("SELECT nombre, telefonos FROM sucursales_reparacion WHERE id = %s", (cotizacion["creador_sucursal_id"],))\n        suc = cur.fetchone()\n        if suc:\n            cotizacion["creador_sucursal_nombre"] = suc["nombre"]\n            cotizacion["creador_sucursal_telefonos"] = suc["telefonos"]\n    return cotizacion'], ['def crear_cotizacion(empresa_id, creado_por_id, cliente_nombre, cliente_direccion, cliente_telefono,\n                      folio_microsip_origen, notas, items, tipo_cliente="publico"):\n    conn = get_connection()\n    cur = conn.cursor()\n    ahora_iso = ahora().isoformat(timespec="seconds")\n    folio = _next_folio_cotizacion(cur, empresa_id)\n    cur.execute("""\n        INSERT INTO cotizaciones (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n                                   folio_microsip_origen, notas, creado_por_id, creado_en, actualizado_en, tipo_cliente)\n        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)\n        RETURNING id\n    """, (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n          folio_microsip_origen, notas, creado_por_id, ahora_iso, ahora_iso, tipo_cliente))\n    cotizacion_id = cur.fetchone()["id"]\n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado\n\n\ndef listar_cotizaciones(empresa_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.empresa_id = %s ORDER BY c.id DESC\n    """, (empresa_id,))\n    cotizaciones = [dict(r) for r in cur.fetchall()]\n    for c in cotizaciones:\n        _enriquecer_cotizacion(cur, c)\n    cur.close(); conn.close()\n    return cotizaciones\n\n\ndef obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=None):\n    conn, cur = _conn_cur if _conn_cur else (get_connection(), None)\n    if cur is None:\n        cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.id = %s AND c.empresa_id = %s\n    """, (cotizacion_id, empresa_id))\n    row = cur.fetchone()\n    resultado = _enriquecer_cotizacion(cur, dict(row)) if row else None\n    if not _conn_cur:\n        cur.close(); conn.close()\n    return resultado\n\n\ndef actualizar_cotizacion(empresa_id, cotizacion_id, cliente_nombre, cliente_direccion, cliente_telefono, notas, items, tipo_cliente="publico"):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("SELECT id FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    if not cur.fetchone():\n        cur.close(); conn.close()\n        return None\n    cur.execute("""\n        UPDATE cotizaciones SET cliente_nombre = %s, cliente_direccion = %s, cliente_telefono = %s,\n                                 notas = %s, actualizado_en = %s, tipo_cliente = %s\n        WHERE id = %s\n    """, (cliente_nombre, cliente_direccion, cliente_telefono, notas,\n          ahora().isoformat(timespec="seconds"), tipo_cliente, cotizacion_id))\n    \n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado', 'def crear_cotizacion(empresa_id, creado_por_id, cliente_nombre, cliente_direccion, cliente_telefono,\n                      folio_microsip_origen, notas, items, tipo_cliente="publico", meses_msi=None):\n    conn = get_connection()\n    cur = conn.cursor()\n    ahora_iso = ahora().isoformat(timespec="seconds")\n    folio = _next_folio_cotizacion(cur, empresa_id)\n    cur.execute("""\n        INSERT INTO cotizaciones (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n                                   folio_microsip_origen, notas, creado_por_id, creado_en, actualizado_en, tipo_cliente, meses_msi)\n        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)\n        RETURNING id\n    """, (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n          folio_microsip_origen, notas, creado_por_id, ahora_iso, ahora_iso, tipo_cliente, meses_msi))\n    cotizacion_id = cur.fetchone()["id"]\n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado\n\n\ndef listar_cotizaciones(empresa_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre,\n               u.telefono_whatsapp AS creador_telefono, u.sucursal_id AS creador_sucursal_id\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.empresa_id = %s ORDER BY c.id DESC\n    """, (empresa_id,))\n    cotizaciones = [dict(r) for r in cur.fetchall()]\n    for c in cotizaciones:\n        _enriquecer_cotizacion(cur, c)\n    cur.close(); conn.close()\n    return cotizaciones\n\n\ndef obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=None):\n    conn, cur = _conn_cur if _conn_cur else (get_connection(), None)\n    if cur is None:\n        cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre,\n               u.telefono_whatsapp AS creador_telefono, u.sucursal_id AS creador_sucursal_id\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.id = %s AND c.empresa_id = %s\n    """, (cotizacion_id, empresa_id))\n    row = cur.fetchone()\n    resultado = _enriquecer_cotizacion(cur, dict(row)) if row else None\n    if not _conn_cur:\n        cur.close(); conn.close()\n    return resultado\n\n\ndef actualizar_cotizacion(empresa_id, cotizacion_id, cliente_nombre, cliente_direccion, cliente_telefono, notas, items, tipo_cliente="publico", meses_msi=None):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("SELECT id FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    if not cur.fetchone():\n        cur.close(); conn.close()\n        return None\n    cur.execute("""\n        UPDATE cotizaciones SET cliente_nombre = %s, cliente_direccion = %s, cliente_telefono = %s,\n                                 notas = %s, actualizado_en = %s, tipo_cliente = %s, meses_msi = %s\n        WHERE id = %s\n    """, (cliente_nombre, cliente_direccion, cliente_telefono, notas,\n          ahora().isoformat(timespec="seconds"), tipo_cliente, meses_msi, cotizacion_id))\n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado']], 'backend/app.py': [['class CotizacionIn(BaseModel):\n    cliente_nombre: str = Field(min_length=1)\n    cliente_direccion: Optional[str] = None\n    cliente_telefono: Optional[str] = None\n    folio_microsip_origen: Optional[str] = None\n    notas: Optional[str] = None\n    tipo_cliente: Literal["publico", "mayoreo", "distribuidor"] = "publico"\n    items: List[CotizacionItemIn] = Field(default_factory=list)', 'class CotizacionIn(BaseModel):\n    cliente_nombre: str = Field(min_length=1)\n    cliente_direccion: Optional[str] = None\n    cliente_telefono: Optional[str] = None\n    folio_microsip_origen: Optional[str] = None\n    notas: Optional[str] = None\n    tipo_cliente: Literal["publico", "mayoreo", "distribuidor"] = "publico"\n    meses_msi: Optional[int] = Field(default=None, ge=0, le=60)\n    items: List[CotizacionItemIn] = Field(default_factory=list)'], ['def api_crear_cotizacion(payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.crear_cotizacion(\n        usuario["empresa_id"], usuario["id"], payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.folio_microsip_origen, payload.notas,\n        [item.model_dump() for item in payload.items], payload.tipo_cliente,\n    )\n\n\n@app.put("/api/cotizaciones/{cotizacion_id}")\ndef api_actualizar_cotizacion(cotizacion_id: int, payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    resultado = db.actualizar_cotizacion(\n        usuario["empresa_id"], cotizacion_id, payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.notas, [item.model_dump() for item in payload.items], payload.tipo_cliente,\n    )\n    if not resultado:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return resultado', 'def api_crear_cotizacion(payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.crear_cotizacion(\n        usuario["empresa_id"], usuario["id"], payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.folio_microsip_origen, payload.notas,\n        [item.model_dump() for item in payload.items], payload.tipo_cliente, payload.meses_msi,\n    )\n\n\n@app.put("/api/cotizaciones/{cotizacion_id}")\ndef api_actualizar_cotizacion(cotizacion_id: int, payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    resultado = db.actualizar_cotizacion(\n        usuario["empresa_id"], cotizacion_id, payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.notas, [item.model_dump() for item in payload.items],\n        payload.tipo_cliente, payload.meses_msi,\n    )\n    if not resultado:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return resultado']], 'frontend/index.html': [["function cot_nuevaVacia() {\n  return { id: null, folio: null, cliente_nombre: '', cliente_direccion: '', cliente_telefono: '', folio_microsip_origen: null, notas: '', tipo_cliente: 'publico', items: [] };\n}", "function cot_nuevaVacia() {\n  return { id: null, folio: null, cliente_nombre: '', cliente_direccion: '', cliente_telefono: '', folio_microsip_origen: null, notas: '', tipo_cliente: 'publico', meses_msi: null, items: [] };\n}"], ["  const tipoEl = document.getElementById('cot_tipo_cliente');\n  const notasEl = document.getElementById('cot_notas');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (tipoEl) COT_ACTUAL.tipo_cliente = tipoEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;", "const tipoEl = document.getElementById('cot_tipo_cliente');\n  const notasEl = document.getElementById('cot_notas');\n  const mesesMsiEl = document.getElementById('cot_meses_msi');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (tipoEl) COT_ACTUAL.tipo_cliente = tipoEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;\n  if (mesesMsiEl) COT_ACTUAL.meses_msi = mesesMsiEl.value ? parseInt(mesesMsiEl.value, 10) : null;"], ['      <div class="field" style="margin-top:16px;"><label>Notas (opcional)</label><textarea id="cot_notas">${escapeHtml(c.notas || \'\')}</textarea></div>', '<div class="field" style="margin-top:16px;"><label>Meses sin intereses (opcional)</label>\n        <input id="cot_meses_msi" type="number" step="1" min="0" max="60" value="${c.meses_msi || \'\'}" placeholder="ej. 6, 9, 12 — vacío si no aplica" />\n        <p style="font-size:11px; color:var(--muted); margin-top:4px;">A 6 meses se cobra el costo íntegro (sin los descuentos por artículo); cada mes arriba de 6 suma 1% de recargo sobre ese mismo costo íntegro.</p>\n      </div>\n      <div class="field" style="margin-top:16px;"><label>Notas (opcional)</label><textarea id="cot_notas">${escapeHtml(c.notas || \'\')}</textarea></div>']]}

PDFS_COTIZACIONES_COMPLETO = '"""Generación del PDF de una cotización (módulo Cotizador, dentro de\nChecador de precio) — mismo estilo membretado que los documentos de\nReparaciones."""\nfrom io import BytesIO\n\nfrom reportlab.lib import colors\nfrom reportlab.lib.pagesizes import letter\nfrom reportlab.lib.styles import ParagraphStyle\nfrom reportlab.lib.units import cm\nfrom reportlab.platypus import Paragraph, Spacer, Table, TableStyle, HRFlowable\n\nfrom pdfs_reparaciones import (\n    ROJO, GRIS, GRIS_CLARO, NEGRO, _styles, _encabezado_membretado, _pie_pagina,\n    _formatear_fecha, _doc_template,\n)\n\n\ndef _fmt_dinero(n):\n    n = float(n or 0)\n    return f"${n:,.2f}"\n\n\ndef calcular_msi(cotizacion):\n    """Meses sin intereses: a 6 meses se cobra el costo íntegro SIN los\n    descuentos por artículo (el descuento no aplica si se difiere a meses);\n    por cada mes arriba de 6 se suma 1% de recargo sobre ese mismo costo\n    íntegro (ej. a 9 meses = 3% de recargo, por los 3 meses arriba del 6º)."""\n    meses = cotizacion.get("meses_msi")\n    if not meses or meses <= 0:\n        return None\n    total_bruto = sum(float(i["cantidad"]) * float(i["precio_unitario"]) for i in cotizacion["items"])\n    recargo_pct = max(0, meses - 6) * 1\n    total_msi = total_bruto * (1 + recargo_pct / 100)\n    return {\n        "meses": meses,\n        "total_bruto": total_bruto,\n        "recargo_pct": recargo_pct,\n        "total_msi": total_msi,\n        "mensualidad": total_msi / meses,\n    }\n\n\nTITULOS_TIPO_CLIENTE = {\n    "publico": "COTIZACIÓN PÚBLICO EN GENERAL",\n    "mayoreo": "COTIZACIÓN MAYOREO",\n    "distribuidor": "COTIZACIÓN DISTRIBUIDOR",\n}\n\n\ndef generar_cotizacion_pdf(cotizacion, empresa):\n    styles = _styles()\n    elementos = []\n    titulo = TITULOS_TIPO_CLIENTE.get(cotizacion.get("tipo_cliente"), "COTIZACIÓN")\n    _encabezado_membretado(\n        elementos, styles, titulo,\n        folio=cotizacion["folio"],\n        fecha=f"Fecha: {_formatear_fecha(cotizacion.get(\'creado_en\'))}",\n        etiqueta_folio="Folio",\n    )\n\n    elementos.append(Paragraph("Cliente", styles["Seccion"]))\n    datos_cliente = [("Nombre", cotizacion.get("cliente_nombre"))]\n    if cotizacion.get("cliente_telefono"):\n        datos_cliente.append(("Teléfono", cotizacion["cliente_telefono"]))\n    if cotizacion.get("cliente_direccion"):\n        datos_cliente.append(("Dirección", cotizacion["cliente_direccion"]))\n    for etiqueta, valor in datos_cliente:\n        elementos.append(Paragraph(f"<b>{etiqueta}:</b> {valor or \'—\'}", styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 10))\n    elementos.append(Paragraph("Artículos cotizados", styles["Seccion"]))\n\n    estilo_celda = ParagraphStyle("CeldaTabla", parent=styles["Normal"], fontSize=9, leading=12)\n    estilo_celda_num = ParagraphStyle("CeldaTablaNum", parent=estilo_celda, alignment=2)\n    estilo_nota = ParagraphStyle("NotaItem", parent=estilo_celda, fontSize=7.5, textColor=GRIS, leftIndent=2)\n    hay_descuentos = any(float(item.get("descuento_pct") or 0) > 0 for item in cotizacion["items"])\n    encabezado = [\n        Paragraph("<b>Artículo</b>", estilo_celda),\n        Paragraph("<b>Cant.</b>", estilo_celda_num),\n        Paragraph("<b>Precio unit.</b>", estilo_celda_num),\n    ]\n    if hay_descuentos:\n        encabezado.append(Paragraph("<b>Desc.</b>", estilo_celda_num))\n    encabezado.append(Paragraph("<b>Subtotal</b>", estilo_celda_num))\n    filas = [encabezado]\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        nombre = item["nombre"] + (f" <font size=7 color=\'#74767A\'>(clave: {item[\'clave\']})</font>" if item.get("clave") else "")\n        if item.get("nota"):\n            nombre_parrafo = [Paragraph(nombre, estilo_celda), Paragraph(f"Nota: {item[\'nota\']}", estilo_nota)]\n        else:\n            nombre_parrafo = Paragraph(nombre, estilo_celda)\n        fila = [\n            nombre_parrafo,\n            Paragraph(f"{cantidad:g}", estilo_celda_num),\n            Paragraph(_fmt_dinero(precio), estilo_celda_num),\n        ]\n        if hay_descuentos:\n            fila.append(Paragraph(f"{descuento_pct:g}%" if descuento_pct else "—", estilo_celda_num))\n        fila.append(Paragraph(_fmt_dinero(subtotal), estilo_celda_num))\n        filas.append(fila)\n\n    if hay_descuentos:\n        colWidths = [7.2 * cm, 1.6 * cm, 2.4 * cm, 1.7 * cm, 2.6 * cm]\n    else:\n        colWidths = [8.8 * cm, 1.8 * cm, 2.7 * cm, 2.7 * cm]\n    tabla = Table(filas, colWidths=colWidths, repeatRows=1)\n    tabla.setStyle(TableStyle([\n        ("BACKGROUND", (0, 0), (-1, 0), ROJO),\n        ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),\n        ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),\n        ("TOPPADDING", (0, 0), (-1, -1), 6),\n        ("BOTTOMPADDING", (0, 0), (-1, -1), 6),\n        ("ROWBACKGROUNDS", (0, 1), (-1, -1), [colors.white, GRIS_CLARO]),\n        ("LINEBELOW", (0, 0), (-1, -1), 0.4, GRIS),\n    ]))\n    elementos.append(tabla)\n\n    elementos.append(Spacer(1, 6))\n    tabla_total = Table([[\n        Paragraph("<b>TOTAL</b>", ParagraphStyle("TotalEtiqueta", parent=styles["Normal"], fontSize=12, textColor=NEGRO)),\n        Paragraph(f"<b>{_fmt_dinero(total)}</b>", ParagraphStyle("TotalValor", parent=styles["Normal"], fontSize=12, alignment=2, textColor=ROJO)),\n    ]], colWidths=[13.3 * cm, 2.7 * cm])\n    tabla_total.setStyle(TableStyle([\n        ("LINEABOVE", (0, 0), (-1, 0), 1.2, ROJO),\n        ("TOPPADDING", (0, 0), (-1, -1), 8),\n    ]))\n    elementos.append(tabla_total)\n\n    msi = calcular_msi(cotizacion)\n    if msi:\n        elementos.append(Spacer(1, 14))\n        elementos.append(Paragraph("Meses sin intereses", styles["Seccion"]))\n        texto_recargo = f" (incluye recargo de {msi[\'recargo_pct\']:g}% por ser más de 6 meses)" if msi["recargo_pct"] else ""\n        elementos.append(Paragraph(\n            f"A <b>{msi[\'meses\']} meses sin intereses</b>{texto_recargo}: total de "\n            f"<b>{_fmt_dinero(msi[\'total_msi\'])}</b> — {msi[\'meses\']} pagos de "\n            f"<b>{_fmt_dinero(msi[\'mensualidad\'])}</b> cada uno.",\n            styles["Cuerpo"],\n        ))\n\n    if cotizacion.get("notas"):\n        elementos.append(Spacer(1, 14))\n        elementos.append(Paragraph("Notas", styles["Seccion"]))\n        elementos.append(Paragraph(cotizacion["notas"], styles["Cuerpo"]))\n\n    contacto_partes = []\n    if cotizacion.get("creado_por_nombre"):\n        tel_creador = f" — Tel. {cotizacion[\'creador_telefono\']}" if cotizacion.get("creador_telefono") else ""\n        contacto_partes.append(f"Atendido por: {cotizacion[\'creado_por_nombre\']}{tel_creador}")\n    if cotizacion.get("creador_sucursal_nombre") and cotizacion.get("creador_sucursal_telefonos"):\n        contacto_partes.append(f"Sucursal {cotizacion[\'creador_sucursal_nombre\']}: {cotizacion[\'creador_sucursal_telefonos\']}")\n    if contacto_partes:\n        elementos.append(Spacer(1, 14))\n        elementos.append(Paragraph("Contacto", styles["Seccion"]))\n        for parte in contacto_partes:\n            elementos.append(Paragraph(parte, styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 20))\n    elementos.append(HRFlowable(width="100%", thickness=0.8, color=GRIS, spaceBefore=4, spaceAfter=8))\n    elementos.append(Paragraph(\n        "Esta cotización es informativa y no representa una factura. Precios sujetos a cambio sin previo aviso; "\n        "vigencia de 15 días naturales salvo que se indique lo contrario.",\n        ParagraphStyle("Vigencia", parent=styles["Normal"], fontSize=7.5, textColor=GRIS),\n    ))\n\n    buffer = BytesIO()\n    documento = _doc_template(buffer)\n    documento.build(elementos, onFirstPage=_pie_pagina, onLaterPages=_pie_pagina)\n    buffer.seek(0)\n    return buffer.read()\n\n\ndef _escapar_html(texto):\n    return (\n        (texto or "")\n        .replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")\n        .replace(\'"\', "&quot;")\n    )\n\n\ndef _fmt_cant(n):\n    n = float(n or 0)\n    return f"{n:g}"\n\n\ndef generar_html_recibo_termico(cotizacion):\n    """Recibo angosto (58mm) para la impresora térmica Star SM-L200, servido\n    en la ruta pública que la app Star PassPRNT consulta directamente (no\n    lleva sesión ni token de la app — por eso nunca incluye datos sensibles\n    de más, solo lo mismo que ya trae la cotización). Se evitan caracteres\n    tipográficos poco comunes (guion en vez de punto medio, etc.) por si la\n    fuente de la impresora no los trae."""\n    filas = ""\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        clave = f" ({_escapar_html(item[\'clave\'])})" if item.get("clave") else ""\n        desc = f" (-{descuento_pct:g}%)" if descuento_pct else ""\n        filas += f"""\n          <tr>\n            <td style="text-align:left; padding:3px 0;">{_escapar_html(item[\'nombre\'])}{clave}<br>{_fmt_cant(cantidad)} x {_fmt_dinero(precio)}{desc}</td>\n            <td style="text-align:right; white-space:nowrap; padding:3px 0;">{_fmt_dinero(subtotal)}</td>\n          </tr>\n        """\n    telefono = f"<br>Tel: {_escapar_html(cotizacion[\'cliente_telefono\'])}" if cotizacion.get("cliente_telefono") else ""\n    return f"""<!DOCTYPE html>\n<html><head><meta charset="utf-8"><title>Cotizacion {_escapar_html(cotizacion[\'folio\'])}</title><style>\n  body {{ width:380px; margin:0; padding:8px; font-family:monospace; font-size:13px; color:#000; }}\n  h1 {{ font-size:16px; text-align:center; margin:4px 0; letter-spacing:1px; }}\n  .centro {{ text-align:center; margin:2px 0; }}\n  .linea {{ border-top:1px dashed #000; margin:8px 0; }}\n  table {{ width:100%; border-collapse:collapse; }}\n  .total td {{ font-size:15px; font-weight:bold; padding-top:6px; }}\n</style></head><body>\n  <h1>MARK - INC</h1>\n  <p class="centro">Cotizacion {_escapar_html(cotizacion[\'folio\'])}</p>\n  <div class="linea"></div>\n  <p><b>Cliente:</b> {_escapar_html(cotizacion[\'cliente_nombre\'])}{telefono}</p>\n  <div class="linea"></div>\n  <table>{filas}</table>\n  <div class="linea"></div>\n  <table><tr class="total"><td>TOTAL</td><td style="text-align:right;">{_fmt_dinero(total)}</td></tr></table>\n  <div class="linea"></div>\n  <p class="centro" style="font-size:10px;">Cotizacion informativa, sujeta a cambios.<br>Vigencia 15 dias.</p>\n</body></html>"""\n'

# Este bloque aparece DOS veces identico en frontend/index.html.
HTML_DOBLE_VIEJO = "    tipo_cliente: COT_ACTUAL.tipo_cliente || 'publico',\n    items: COT_ACTUAL.items.map(it => ({"
HTML_DOBLE_NUEVO = "    tipo_cliente: COT_ACTUAL.tipo_cliente || 'publico',\n    meses_msi: COT_ACTUAL.meses_msi || null,\n    items: COT_ACTUAL.items.map(it => ({"


def leer(ruta):
    with open(ruta, "r", encoding="utf-8", newline=None) as f:
        return f.read()


def escribir(ruta, contenido):
    with open(ruta, "w", encoding="utf-8", newline="") as f:
        f.write(contenido)


def aplicar_reemplazos(ruta):
    reemplazos = ARCHIVOS[ruta]
    try:
        contenido = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    cambios = 0
    hubo_error = False
    for viejo, nuevo in reemplazos:
        if nuevo in contenido:
            continue
        if viejo not in contenido:
            print(f"[{ruta}] No encontre un bloque esperado (el archivo pudo haber cambiado). Avisale a Claude.")
            hubo_error = True
            continue
        contenido = contenido.replace(viejo, nuevo, 1)
        cambios += 1

    if ruta == "frontend/index.html":
        ocurrencias = contenido.count(HTML_DOBLE_VIEJO)
        if ocurrencias > 0:
            contenido = contenido.replace(HTML_DOBLE_VIEJO, HTML_DOBLE_NUEVO)
            cambios += ocurrencias
        elif contenido.count(HTML_DOBLE_NUEVO) < 2:
            print(f"[{ruta}] No encontre las 2 copias esperadas del payload de guardado. Avisale a Claude.")
            hubo_error = True

    escribir(ruta, contenido)
    print(f"[{ruta}] {cambios} cambio(s) aplicado(s).")
    return not hubo_error


def aplicar_pdfs_cotizaciones():
    ruta = "backend/pdfs_cotizaciones.py"
    try:
        actual = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    if actual.strip() == PDFS_COTIZACIONES_COMPLETO.strip():
        print(f"[{ruta}] Ya esta correcto. No hace falta nada.")
        return True
    escribir(ruta, PDFS_COTIZACIONES_COMPLETO)
    print(f"[{ruta}] Actualizado completo (MSI + pie de contacto).")
    return True


def main():
    ok = True
    ok = aplicar_pdfs_cotizaciones() and ok
    ok = aplicar_reemplazos("backend/db.py") and ok
    ok = aplicar_reemplazos("backend/app.py") and ok
    ok = aplicar_reemplazos("frontend/index.html") and ok

    if not ok:
        print()
        print("Algo no se pudo aplicar automaticamente. Avisale a Claude que mensaje salio, sin correr git add/commit todavia.")
        sys.exit(1)

    print()
    print("Todo listo. Ahora corre:")
    print("   git add backend/app.py backend/db.py backend/pdfs_cotizaciones.py frontend/index.html")
    print('   git commit -m "Cotizador: pie de contacto (usuario+sucursal) y meses sin intereses"')
    print("   git push")


if __name__ == "__main__":
    main()
