# -*- coding: utf-8 -*-
"""
Cotizador: agrega descuento por articulo (%), nota por articulo (se imprime
en el PDF), y un boton para enviar la cotizacion por WhatsApp (liga publica
y corta al PDF, abre WhatsApp con el mensaje ya redactado).

Uso: colocalo en la carpeta del repo (junto a backend/ y frontend/) y corre:
    py fix_descuento_whatsapp.py
"""
import sys

ARCHIVOS = {'backend/db.py': [['CREATE TABLE IF NOT EXISTS cotizacion_items (\n            id SERIAL PRIMARY KEY,\n            cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id) ON DELETE CASCADE,\n            articulo_id INTEGER,\n            clave TEXT,\n            nombre TEXT NOT NULL,\n            cantidad NUMERIC NOT NULL DEFAULT 1,\n            precio_unitario NUMERIC NOT NULL DEFAULT 0,\n            orden INTEGER NOT NULL DEFAULT 0\n        );', 'CREATE TABLE IF NOT EXISTS cotizacion_items (\n            id SERIAL PRIMARY KEY,\n            cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id) ON DELETE CASCADE,\n            articulo_id INTEGER,\n            clave TEXT,\n            nombre TEXT NOT NULL,\n            cantidad NUMERIC NOT NULL DEFAULT 1,\n            precio_unitario NUMERIC NOT NULL DEFAULT 0,\n            orden INTEGER NOT NULL DEFAULT 0,\n            descuento_pct NUMERIC NOT NULL DEFAULT 0,\n            nota TEXT\n        );'], ['        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS token_impresion TEXT UNIQUE;\n    """)', '        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS token_impresion TEXT UNIQUE;\n        ALTER TABLE cotizacion_items ADD COLUMN IF NOT EXISTS descuento_pct NUMERIC NOT NULL DEFAULT 0;\n        ALTER TABLE cotizacion_items ADD COLUMN IF NOT EXISTS nota TEXT;\n    """)'], ['def _guardar_items_cotizacion(cur, cotizacion_id, items):\n    cur.execute("DELETE FROM cotizacion_items WHERE cotizacion_id = %s", (cotizacion_id,))\n    for i, item in enumerate(items):\n        cur.execute("""\n            INSERT INTO cotizacion_items (cotizacion_id, articulo_id, clave, nombre, cantidad, precio_unitario, orden)\n            VALUES (%s, %s, %s, %s, %s, %s, %s)\n        """, (cotizacion_id, item.get("articulo_id"), item.get("clave"), item["nombre"],\n              item["cantidad"], item["precio_unitario"], i))\n\n\ndef _enriquecer_cotizacion(cur, cotizacion):\n    cur.execute("""\n        SELECT id, articulo_id, clave, nombre, cantidad, precio_unitario\n        FROM cotizacion_items WHERE cotizacion_id = %s ORDER BY orden\n    """, (cotizacion["id"],))\n    cotizacion["items"] = [dict(r) for r in cur.fetchall()]\n    cotizacion["total"] = sum(float(i["cantidad"]) * float(i["precio_unitario"]) for i in cotizacion["items"])\n    return cotizacion', 'def _guardar_items_cotizacion(cur, cotizacion_id, items):\n    cur.execute("DELETE FROM cotizacion_items WHERE cotizacion_id = %s", (cotizacion_id,))\n    for i, item in enumerate(items):\n        cur.execute("""\n            INSERT INTO cotizacion_items (cotizacion_id, articulo_id, clave, nombre, cantidad, precio_unitario, orden, descuento_pct, nota)\n            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)\n        """, (cotizacion_id, item.get("articulo_id"), item.get("clave"), item["nombre"],\n              item["cantidad"], item["precio_unitario"], i, item.get("descuento_pct") or 0, item.get("nota") or None))\n\n\ndef _subtotal_item(item):\n    """Cantidad x precio unitario, con el descuento por artículo (%) ya aplicado."""\n    bruto = float(item["cantidad"]) * float(item["precio_unitario"])\n    descuento_pct = float(item.get("descuento_pct") or 0)\n    return bruto * (1 - descuento_pct / 100)\n\n\ndef _enriquecer_cotizacion(cur, cotizacion):\n    cur.execute("""\n        SELECT id, articulo_id, clave, nombre, cantidad, precio_unitario, descuento_pct, nota\n        FROM cotizacion_items WHERE cotizacion_id = %s ORDER BY orden\n    """, (cotizacion["id"],))\n    cotizacion["items"] = [dict(r) for r in cur.fetchall()]\n    cotizacion["total"] = sum(_subtotal_item(i) for i in cotizacion["items"])\n    return cotizacion']], 'backend/app.py': [['class CotizacionItemIn(BaseModel):\n    articulo_id: Optional[int] = None\n    clave: Optional[str] = None\n    nombre: str = Field(min_length=1)\n    cantidad: float = Field(gt=0)\n    precio_unitario: float = Field(ge=0)', 'class CotizacionItemIn(BaseModel):\n    articulo_id: Optional[int] = None\n    clave: Optional[str] = None\n    nombre: str = Field(min_length=1)\n    cantidad: float = Field(gt=0)\n    precio_unitario: float = Field(ge=0)\n    descuento_pct: float = Field(default=0, ge=0, le=100)\n    nota: Optional[str] = None'], ['@app.get("/recibo-cotizacion/{token}")\ndef api_recibo_cotizacion_publico(token: str):\n    """Ruta PÚBLICA (sin login) — la consulta directamente la app Star\n    PassPRNT para traer el recibo a imprimir. Token corto, aleatorio, y de\n    un solo uso por impresión (se regenera cada vez que se pide imprimir)."""\n    cotizacion = db.obtener_cotizacion_por_token_impresion(token)\n    if not cotizacion:\n        return Response(content="<p>Esta liga de impresión ya no es válida — vuelve a la cotización e imprime de nuevo.</p>",\n                         media_type="text/html; charset=utf-8", status_code=404)\n    html = pdfs_cotizaciones.generar_html_recibo_termico(cotizacion)\n    return Response(content=html, media_type="text/html; charset=utf-8")', '@app.get("/recibo-cotizacion/{token}")\ndef api_recibo_cotizacion_publico(token: str):\n    """Ruta PÚBLICA (sin login) — la consulta directamente la app Star\n    PassPRNT para traer el recibo a imprimir. Token corto, aleatorio, y de\n    un solo uso por impresión (se regenera cada vez que se pide imprimir)."""\n    cotizacion = db.obtener_cotizacion_por_token_impresion(token)\n    if not cotizacion:\n        return Response(content="<p>Esta liga de impresión ya no es válida — vuelve a la cotización e imprime de nuevo.</p>",\n                         media_type="text/html; charset=utf-8", status_code=404)\n    html = pdfs_cotizaciones.generar_html_recibo_termico(cotizacion)\n    return Response(content=html, media_type="text/html; charset=utf-8")\n\n\n@app.get("/cotizacion-pdf/{token}")\ndef api_cotizacion_pdf_publico(token: str):\n    """Ruta PÚBLICA (sin login) — el mismo token de /recibo-cotizacion/ pero\n    para poder mandar la cotización en PDF por WhatsApp (el destinatario no\n    tiene sesión en la app, necesita poder abrir la liga directo)."""\n    cotizacion = db.obtener_cotizacion_por_token_impresion(token)\n    if not cotizacion:\n        return Response(content="Esta liga ya no es válida — vuelve a la cotización y genera el envío de nuevo.",\n                         media_type="text/plain; charset=utf-8", status_code=404)\n    empresa = db.obtener_empresa(cotizacion["empresa_id"])\n    pdf_bytes = pdfs_cotizaciones.generar_cotizacion_pdf(cotizacion, empresa)\n    return Response(content=pdf_bytes, media_type="application/pdf",\n                     headers={"Content-Disposition": f"inline; filename=cotizacion_{cotizacion[\'folio\']}.pdf"})']], 'frontend/index.html': [["function cot_total() {\n  return COT_ACTUAL.items.reduce((suma, it) => suma + (Number(it.cantidad) || 0) * (Number(it.precio_unitario) || 0), 0);\n}\n\n// Antes de cualquier acción que vuelva a dibujar el formulario (agregar/quitar\n// artículo, guardar), se leen los inputs que el usuario ya haya tocado —\n// para no perder ediciones a medio hacer en otras filas.\nfunction cot_sincronizarDesdeInputs() {\n  if (!COT_ACTUAL) return;\n  const nombreEl = document.getElementById('cot_cliente_nombre');\n  const dirEl = document.getElementById('cot_cliente_direccion');\n  const telEl = document.getElementById('cot_cliente_telefono');\n  const notasEl = document.getElementById('cot_notas');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;\n  COT_ACTUAL.items.forEach((it, i) => {\n    const n = document.getElementById(`cot_item_nombre_${i}`);\n    const c = document.getElementById(`cot_item_cant_${i}`);\n    const p = document.getElementById(`cot_item_precio_${i}`);\n    if (n) it.nombre = n.value;\n    if (c) it.cantidad = parseFloat(c.value) || 0;\n    if (p) it.precio_unitario = parseFloat(p.value) || 0;\n  });\n}\n\nfunction cot_actualizarTotalEnPantalla() {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.forEach((it, i) => {\n    const sub = document.getElementById(`cot_item_subtotal_${i}`);\n    if (sub) sub.textContent = '$' + cot_fmt((Number(it.cantidad) || 0) * (Number(it.precio_unitario) || 0));\n  });\n  const totalEl = document.getElementById('cot_total');\n  if (totalEl) totalEl.textContent = '$' + cot_fmt(cot_total());\n}\n", "function cot_subtotalItem(it) {\n  const bruto = (Number(it.cantidad) || 0) * (Number(it.precio_unitario) || 0);\n  const descuento = Number(it.descuento_pct) || 0;\n  return bruto * (1 - descuento / 100);\n}\n\nfunction cot_total() {\n  return COT_ACTUAL.items.reduce((suma, it) => suma + cot_subtotalItem(it), 0);\n}\n\n// Antes de cualquier acción que vuelva a dibujar el formulario (agregar/quitar\n// artículo, guardar), se leen los inputs que el usuario ya haya tocado —\n// para no perder ediciones a medio hacer en otras filas.\nfunction cot_sincronizarDesdeInputs() {\n  if (!COT_ACTUAL) return;\n  const nombreEl = document.getElementById('cot_cliente_nombre');\n  const dirEl = document.getElementById('cot_cliente_direccion');\n  const telEl = document.getElementById('cot_cliente_telefono');\n  const notasEl = document.getElementById('cot_notas');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;\n  COT_ACTUAL.items.forEach((it, i) => {\n    const n = document.getElementById(`cot_item_nombre_${i}`);\n    const c = document.getElementById(`cot_item_cant_${i}`);\n    const p = document.getElementById(`cot_item_precio_${i}`);\n    const d = document.getElementById(`cot_item_desc_${i}`);\n    const nt = document.getElementById(`cot_item_nota_${i}`);\n    if (n) it.nombre = n.value;\n    if (c) it.cantidad = parseFloat(c.value) || 0;\n    if (p) it.precio_unitario = parseFloat(p.value) || 0;\n    if (d) it.descuento_pct = parseFloat(d.value) || 0;\n    if (nt) it.nota = nt.value;\n  });\n}\n\nfunction cot_actualizarTotalEnPantalla() {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.forEach((it, i) => {\n    const sub = document.getElementById(`cot_item_subtotal_${i}`);\n    if (sub) sub.textContent = '$' + cot_fmt(cot_subtotalItem(it));\n  });\n  const totalEl = document.getElementById('cot_total');\n  if (totalEl) totalEl.textContent = '$' + cot_fmt(cot_total());\n}\n"], ['    ${c.id ? `\n      <div style="display:flex; gap:8px; margin-bottom:16px;">\n        <button class="secondary" style="flex:1;" onclick="cot_descargarPdfActual()">📄 Descargar PDF</button>\n        <button class="secondary" style="flex:1;" onclick="cot_imprimirBluetoothActual()">🖨️ Imprimir en Star (Bluetooth)</button>\n      </div>\n    ` : \'\'}', '    ${c.id ? `\n      <div style="display:flex; gap:8px; margin-bottom:16px; flex-wrap:wrap;">\n        <button class="secondary" style="flex:1; min-width:140px;" onclick="cot_descargarPdfActual()">📄 Descargar PDF</button>\n        <button class="secondary" style="flex:1; min-width:140px;" onclick="cot_imprimirBluetoothActual()">🖨️ Imprimir en Star</button>\n        <button class="secondary" style="flex:1; min-width:140px;" onclick="cot_enviarWhatsAppActual()">💬 Enviar por WhatsApp</button>\n      </div>\n    ` : \'\'}'], ['<label style="font-size:11px; color:var(--muted); text-transform:uppercase; display:block; margin-top:18px;">Artículos</label>\n    <table class="users" style="margin-top:6px;">\n      <thead><tr><th>Artículo</th><th style="width:90px;">Cantidad</th><th style="width:120px;">Precio unit.</th><th style="width:100px;">Subtotal</th><th></th></tr></thead>\n      <tbody>\n        ${c.items.map((it, i) => `\n          <tr>\n            <td>\n              <input id="cot_item_nombre_${i}" value="${escapeHtml(it.nombre)}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" />\n              ${it.clave ? `<div style="font-size:10px; color:var(--muted); margin-top:2px;">Microsip: ${escapeHtml(it.clave)}</div>` : \'\'}\n            </td>\n            <td><input id="cot_item_cant_${i}" type="number" step="any" min="0" value="${it.cantidad}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_precio_${i}" type="number" step="0.01" min="0" value="${it.precio_unitario}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td id="cot_item_subtotal_${i}">$${cot_fmt((Number(it.cantidad)||0) * (Number(it.precio_unitario)||0))}</td>\n            <td><button type="button" class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_quitarItem(${i})">✕</button></td>\n          </tr>\n        `).join(\'\') || `<tr><td colspan="5" class="empty-col">— sin artículos todavía —</td></tr>`}\n      </tbody>\n    </table>\n    <p style="text-align:right; font-size:16px; margin-top:8px;"><b>Total: <span id="cot_total">$${cot_fmt(cot_total())}</span></b></p>\n\n    ', '<label style="font-size:11px; color:var(--muted); text-transform:uppercase; display:block; margin-top:18px;">Artículos</label>\n    <table class="users" style="margin-top:6px;">\n      <thead><tr><th>Artículo</th><th style="width:80px;">Cantidad</th><th style="width:110px;">Precio unit.</th><th style="width:70px;">Desc. %</th><th style="width:100px;">Subtotal</th><th></th></tr></thead>\n      <tbody>\n        ${c.items.map((it, i) => `\n          <tr>\n            <td>\n              <input id="cot_item_nombre_${i}" value="${escapeHtml(it.nombre)}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" />\n              ${it.clave ? `<div style="font-size:10px; color:var(--muted); margin-top:2px;">Microsip: ${escapeHtml(it.clave)}</div>` : \'\'}\n              <input id="cot_item_nota_${i}" value="${escapeHtml(it.nota || \'\')}" placeholder="Nota de este artículo (opcional, sale en el PDF)" style="width:100%; font-size:11px; margin-top:4px; padding:4px 6px;" />\n            </td>\n            <td><input id="cot_item_cant_${i}" type="number" step="any" min="0" value="${it.cantidad}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_precio_${i}" type="number" step="0.01" min="0" value="${it.precio_unitario}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_desc_${i}" type="number" step="any" min="0" max="100" value="${it.descuento_pct || 0}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td id="cot_item_subtotal_${i}">$${cot_fmt(cot_subtotalItem(it))}</td>\n            <td><button type="button" class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_quitarItem(${i})">✕</button></td>\n          </tr>\n        `).join(\'\') || `<tr><td colspan="6" class="empty-col">— sin artículos todavía —</td></tr>`}\n      </tbody>\n    </table>\n    <p style="text-align:right; font-size:16px; margin-top:8px;"><b>Total: <span id="cot_total">$${cot_fmt(cot_total())}</span></b></p>\n\n    '], ["function cot_agregarItemDesdeArticulo(datos) {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({\n    articulo_id: datos.articulo_id,\n    clave: (datos.claves && datos.claves[0]) || null,\n    nombre: datos.nombre,\n    cantidad: 1,\n    precio_unitario: datos.precio_con_impuesto || 0,\n  });\n  renderCotizadorForm();\n}\n\nfunction cot_agregarItemManual() {\n  const nombre = document.getElementById('cot_manual_nombre').value.trim();\n  const cantidad = parseFloat(document.getElementById('cot_manual_cantidad').value) || 0;\n  const precio = parseFloat(document.getElementById('cot_manual_precio').value) || 0;\n  if (!nombre) { document.getElementById('cot_manual_nombre').focus(); return; }\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({ articulo_id: null, clave: null, nombre, cantidad: cantidad || 1, precio_unitario: precio });\n  renderCotizadorForm();\n}\n", "function cot_agregarItemDesdeArticulo(datos) {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({\n    articulo_id: datos.articulo_id,\n    clave: (datos.claves && datos.claves[0]) || null,\n    nombre: datos.nombre,\n    cantidad: 1,\n    precio_unitario: datos.precio_con_impuesto || 0,\n    descuento_pct: 0,\n    nota: '',\n  });\n  renderCotizadorForm();\n}\n\nfunction cot_agregarItemManual() {\n  const nombre = document.getElementById('cot_manual_nombre').value.trim();\n  const cantidad = parseFloat(document.getElementById('cot_manual_cantidad').value) || 0;\n  const precio = parseFloat(document.getElementById('cot_manual_precio').value) || 0;\n  if (!nombre) { document.getElementById('cot_manual_nombre').focus(); return; }\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({ articulo_id: null, clave: null, nombre, cantidad: cantidad || 1, precio_unitario: precio, descuento_pct: 0, nota: '' });\n  renderCotizadorForm();\n}\n"], ["function cot_imprimirBluetoothActual() {\n  if (!COT_ACTUAL || !COT_ACTUAL.id) return;\n  cot_guardarCambiosActuales()\n    .then(() => cot_abrirPassPRNT(COT_ACTUAL.id))\n    .catch(e => alert('No se pudo guardar antes de imprimir: ' + e.message));\n}\n", "function cot_imprimirBluetoothActual() {\n  if (!COT_ACTUAL || !COT_ACTUAL.id) return;\n  cot_guardarCambiosActuales()\n    .then(() => cot_abrirPassPRNT(COT_ACTUAL.id))\n    .catch(e => alert('No se pudo guardar antes de imprimir: ' + e.message));\n}\n\n// ---- Enviar por WhatsApp ----\n// Se manda una liga corta y pública al PDF (el mismo token de la impresión\n// Bluetooth) dentro de un mensaje de WhatsApp ya redactado — al abrir la\n// liga, WhatsApp se abre directo con el mensaje listo para mandar.\n\nfunction cot_abrirWhatsApp(cotizacion) {\n  api(`/api/cotizaciones/${cotizacion.id}/liga-impresion`, { method: 'POST' }).then(({ token }) => {\n    const pdfUrl = `${window.location.origin}/cotizacion-pdf/${token}`;\n    const texto = `Hola ${cotizacion.cliente_nombre}, te comparto tu cotización ${cotizacion.folio} por un total de $${cot_fmt(cotizacion.total)}. Puedes verla aquí: ${pdfUrl}`;\n    const soloDigitos = (cotizacion.cliente_telefono || '').replace(/\\D/g, '');\n    // Si son 10 dígitos se asume número mexicano (se le antepone 52); si ya\n    // trae más dígitos (con lada de país) se manda tal cual; si no hay\n    // teléfono capturado, WhatsApp deja elegir el contacto a mano.\n    const numero = soloDigitos.length === 10 ? `52${soloDigitos}` : (soloDigitos.length > 10 ? soloDigitos : '');\n    const waUrl = numero\n      ? `https://wa.me/${numero}?text=${encodeURIComponent(texto)}`\n      : `https://wa.me/?text=${encodeURIComponent(texto)}`;\n    window.open(waUrl, '_blank');\n  }).catch(e => alert('No se pudo preparar el envío: ' + e.message));\n}\n\nasync function cot_enviarWhatsAppLista(id) {\n  try {\n    const cotizacion = await api(`/api/cotizaciones/${id}`);\n    cot_abrirWhatsApp(cotizacion);\n  } catch (e) {\n    alert(e.message);\n  }\n}\n\nfunction cot_enviarWhatsAppActual() {\n  if (!COT_ACTUAL || !COT_ACTUAL.id) return;\n  cot_guardarCambiosActuales()\n    .then(() => cot_abrirWhatsApp(COT_ACTUAL))\n    .catch(e => alert('No se pudo guardar antes de enviar: ' + e.message));\n}\n"], ['<button class="secondary" style="padding:4px 8px;" onclick="cot_imprimirBluetoothLista(${c.id})">🖨️ Star</button>', '<button class="secondary" style="padding:4px 8px;" onclick="cot_imprimirBluetoothLista(${c.id})">🖨️ Star</button>\n              <button class="secondary" style="padding:4px 8px;" onclick="cot_enviarWhatsAppLista(${c.id})">💬 WhatsApp</button>']]}

PDFS_COTIZACIONES_CONTENIDO_COMPLETO = '"""Generación del PDF de una cotización (módulo Cotizador, dentro de\nChecador de precio) — mismo estilo membretado que los documentos de\nReparaciones."""\nfrom io import BytesIO\n\nfrom reportlab.lib import colors\nfrom reportlab.lib.pagesizes import letter\nfrom reportlab.lib.styles import ParagraphStyle\nfrom reportlab.lib.units import cm\nfrom reportlab.platypus import Paragraph, Spacer, Table, TableStyle, HRFlowable\n\nfrom pdfs_reparaciones import (\n    ROJO, GRIS, GRIS_CLARO, NEGRO, _styles, _encabezado_membretado, _pie_pagina,\n    _formatear_fecha, _doc_template,\n)\n\n\ndef _fmt_dinero(n):\n    n = float(n or 0)\n    return f"${n:,.2f}"\n\n\ndef generar_cotizacion_pdf(cotizacion, empresa):\n    styles = _styles()\n    elementos = []\n    _encabezado_membretado(\n        elementos, styles, "COTIZACIÓN",\n        folio=cotizacion["folio"],\n        fecha=f"Fecha: {_formatear_fecha(cotizacion.get(\'creado_en\'))}",\n        etiqueta_folio="Folio",\n    )\n\n    elementos.append(Paragraph("Cliente", styles["Seccion"]))\n    datos_cliente = [("Nombre", cotizacion.get("cliente_nombre"))]\n    if cotizacion.get("cliente_telefono"):\n        datos_cliente.append(("Teléfono", cotizacion["cliente_telefono"]))\n    if cotizacion.get("cliente_direccion"):\n        datos_cliente.append(("Dirección", cotizacion["cliente_direccion"]))\n    for etiqueta, valor in datos_cliente:\n        elementos.append(Paragraph(f"<b>{etiqueta}:</b> {valor or \'—\'}", styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 10))\n    elementos.append(Paragraph("Artículos cotizados", styles["Seccion"]))\n\n    estilo_celda = ParagraphStyle("CeldaTabla", parent=styles["Normal"], fontSize=9, leading=12)\n    estilo_celda_num = ParagraphStyle("CeldaTablaNum", parent=estilo_celda, alignment=2)\n    estilo_nota = ParagraphStyle("NotaItem", parent=estilo_celda, fontSize=7.5, textColor=GRIS, leftIndent=2)\n    hay_descuentos = any(float(item.get("descuento_pct") or 0) > 0 for item in cotizacion["items"])\n    encabezado = [\n        Paragraph("<b>Artículo</b>", estilo_celda),\n        Paragraph("<b>Cant.</b>", estilo_celda_num),\n        Paragraph("<b>Precio unit.</b>", estilo_celda_num),\n    ]\n    if hay_descuentos:\n        encabezado.append(Paragraph("<b>Desc.</b>", estilo_celda_num))\n    encabezado.append(Paragraph("<b>Subtotal</b>", estilo_celda_num))\n    filas = [encabezado]\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        nombre = item["nombre"] + (f" <font size=7 color=\'#74767A\'>(clave: {item[\'clave\']})</font>" if item.get("clave") else "")\n        if item.get("nota"):\n            nombre_parrafo = [Paragraph(nombre, estilo_celda), Paragraph(f"Nota: {item[\'nota\']}", estilo_nota)]\n        else:\n            nombre_parrafo = Paragraph(nombre, estilo_celda)\n        fila = [\n            nombre_parrafo,\n            Paragraph(f"{cantidad:g}", estilo_celda_num),\n            Paragraph(_fmt_dinero(precio), estilo_celda_num),\n        ]\n        if hay_descuentos:\n            fila.append(Paragraph(f"{descuento_pct:g}%" if descuento_pct else "—", estilo_celda_num))\n        fila.append(Paragraph(_fmt_dinero(subtotal), estilo_celda_num))\n        filas.append(fila)\n\n    if hay_descuentos:\n        colWidths = [7.2 * cm, 1.6 * cm, 2.4 * cm, 1.7 * cm, 2.6 * cm]\n    else:\n        colWidths = [8.8 * cm, 1.8 * cm, 2.7 * cm, 2.7 * cm]\n    tabla = Table(filas, colWidths=colWidths, repeatRows=1)\n    tabla.setStyle(TableStyle([\n        ("BACKGROUND", (0, 0), (-1, 0), ROJO),\n        ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),\n        ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),\n        ("TOPPADDING", (0, 0), (-1, -1), 6),\n        ("BOTTOMPADDING", (0, 0), (-1, -1), 6),\n        ("ROWBACKGROUNDS", (0, 1), (-1, -1), [colors.white, GRIS_CLARO]),\n        ("LINEBELOW", (0, 0), (-1, -1), 0.4, GRIS),\n    ]))\n    elementos.append(tabla)\n\n    elementos.append(Spacer(1, 6))\n    tabla_total = Table([[\n        Paragraph("<b>TOTAL</b>", ParagraphStyle("TotalEtiqueta", parent=styles["Normal"], fontSize=12, textColor=NEGRO)),\n        Paragraph(f"<b>{_fmt_dinero(total)}</b>", ParagraphStyle("TotalValor", parent=styles["Normal"], fontSize=12, alignment=2, textColor=ROJO)),\n    ]], colWidths=[13.3 * cm, 2.7 * cm])\n    tabla_total.setStyle(TableStyle([\n        ("LINEABOVE", (0, 0), (-1, 0), 1.2, ROJO),\n        ("TOPPADDING", (0, 0), (-1, -1), 8),\n    ]))\n    elementos.append(tabla_total)\n\n    if cotizacion.get("notas"):\n        elementos.append(Spacer(1, 14))\n        elementos.append(Paragraph("Notas", styles["Seccion"]))\n        elementos.append(Paragraph(cotizacion["notas"], styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 20))\n    elementos.append(HRFlowable(width="100%", thickness=0.8, color=GRIS, spaceBefore=4, spaceAfter=8))\n    elementos.append(Paragraph(\n        "Esta cotización es informativa y no representa una factura. Precios sujetos a cambio sin previo aviso; "\n        "vigencia de 15 días naturales salvo que se indique lo contrario.",\n        ParagraphStyle("Vigencia", parent=styles["Normal"], fontSize=7.5, textColor=GRIS),\n    ))\n\n    buffer = BytesIO()\n    documento = _doc_template(buffer)\n    documento.build(elementos, onFirstPage=_pie_pagina, onLaterPages=_pie_pagina)\n    buffer.seek(0)\n    return buffer.read()\n\n\ndef _escapar_html(texto):\n    return (\n        (texto or "")\n        .replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")\n        .replace(\'"\', "&quot;")\n    )\n\n\ndef _fmt_cant(n):\n    n = float(n or 0)\n    return f"{n:g}"\n\n\ndef generar_html_recibo_termico(cotizacion):\n    """Recibo angosto (58mm) para la impresora térmica Star SM-L200, servido\n    en la ruta pública que la app Star PassPRNT consulta directamente (no\n    lleva sesión ni token de la app — por eso nunca incluye datos sensibles\n    de más, solo lo mismo que ya trae la cotización). Se evitan caracteres\n    tipográficos poco comunes (guion en vez de punto medio, etc.) por si la\n    fuente de la impresora no los trae."""\n    filas = ""\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        clave = f" ({_escapar_html(item[\'clave\'])})" if item.get("clave") else ""\n        desc = f" (-{descuento_pct:g}%)" if descuento_pct else ""\n        filas += f"""\n          <tr>\n            <td style="text-align:left; padding:3px 0;">{_escapar_html(item[\'nombre\'])}{clave}<br>{_fmt_cant(cantidad)} x {_fmt_dinero(precio)}{desc}</td>\n            <td style="text-align:right; white-space:nowrap; padding:3px 0;">{_fmt_dinero(subtotal)}</td>\n          </tr>\n        """\n    telefono = f"<br>Tel: {_escapar_html(cotizacion[\'cliente_telefono\'])}" if cotizacion.get("cliente_telefono") else ""\n    return f"""<!DOCTYPE html>\n<html><head><meta charset="utf-8"><title>Cotizacion {_escapar_html(cotizacion[\'folio\'])}</title><style>\n  body {{ width:380px; margin:0; padding:8px; font-family:monospace; font-size:13px; color:#000; }}\n  h1 {{ font-size:16px; text-align:center; margin:4px 0; letter-spacing:1px; }}\n  .centro {{ text-align:center; margin:2px 0; }}\n  .linea {{ border-top:1px dashed #000; margin:8px 0; }}\n  table {{ width:100%; border-collapse:collapse; }}\n  .total td {{ font-size:15px; font-weight:bold; padding-top:6px; }}\n</style></head><body>\n  <h1>MARK - INC</h1>\n  <p class="centro">Cotizacion {_escapar_html(cotizacion[\'folio\'])}</p>\n  <div class="linea"></div>\n  <p><b>Cliente:</b> {_escapar_html(cotizacion[\'cliente_nombre\'])}{telefono}</p>\n  <div class="linea"></div>\n  <table>{filas}</table>\n  <div class="linea"></div>\n  <table><tr class="total"><td>TOTAL</td><td style="text-align:right;">{_fmt_dinero(total)}</td></tr></table>\n  <div class="linea"></div>\n  <p class="centro" style="font-size:10px;">Cotizacion informativa, sujeta a cambios.<br>Vigencia 15 dias.</p>\n</body></html>"""\n'

# Este bloque aparece DOS veces identico en frontend/index.html (el payload
# de guardar se arma igual en dos funciones distintas) -- se reemplazan
# las dos ocurrencias, no solo la primera.
HTML_DOBLE_VIEJO = '    items: COT_ACTUAL.items.map(it => ({\n      articulo_id: it.articulo_id || null, clave: it.clave || null, nombre: it.nombre,\n      cantidad: Number(it.cantidad) || 0, precio_unitario: Number(it.precio_unitario) || 0,\n    })),'
HTML_DOBLE_NUEVO = '    items: COT_ACTUAL.items.map(it => ({\n      articulo_id: it.articulo_id || null, clave: it.clave || null, nombre: it.nombre,\n      cantidad: Number(it.cantidad) || 0, precio_unitario: Number(it.precio_unitario) || 0,\n      descuento_pct: Number(it.descuento_pct) || 0, nota: it.nota || null,\n    })),'


def leer(ruta):
    with open(ruta, "r", encoding="utf-8", newline=None) as f:
        return f.read()


def escribir(ruta, contenido):
    with open(ruta, "w", encoding="utf-8", newline="") as f:
        f.write(contenido)


def aplicar_reemplazos(ruta):
    reemplazos = ARCHIVOS[ruta]
    try:
        contenido = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    cambios = 0
    hubo_error = False
    for viejo, nuevo in reemplazos:
        if nuevo in contenido:
            continue
        if viejo not in contenido:
            print(f"[{ruta}] No encontre un bloque esperado (el archivo pudo haber cambiado). Avisale a Claude.")
            hubo_error = True
            continue
        contenido = contenido.replace(viejo, nuevo, 1)
        cambios += 1

    if ruta == "frontend/index.html":
        ocurrencias = contenido.count(HTML_DOBLE_VIEJO)
        if ocurrencias > 0:
            contenido = contenido.replace(HTML_DOBLE_VIEJO, HTML_DOBLE_NUEVO)
            cambios += ocurrencias
        elif HTML_DOBLE_NUEVO not in contenido or contenido.count(HTML_DOBLE_NUEVO) < 2:
            print(f"[{ruta}] No encontre las 2 copias esperadas del payload de guardado. Avisale a Claude.")
            hubo_error = True

    escribir(ruta, contenido)
    print(f"[{ruta}] {cambios} cambio(s) aplicado(s).")
    return not hubo_error


def aplicar_pdfs_cotizaciones():
    ruta = "backend/pdfs_cotizaciones.py"
    try:
        actual = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    if actual.strip() == PDFS_COTIZACIONES_CONTENIDO_COMPLETO.strip():
        print(f"[{ruta}] Ya esta correcto. No hace falta nada.")
        return True
    escribir(ruta, PDFS_COTIZACIONES_CONTENIDO_COMPLETO)
    print(f"[{ruta}] Actualizado completo (descuento y nota por articulo en el PDF).")
    return True


def main():
    ok = True
    ok = aplicar_pdfs_cotizaciones() and ok
    ok = aplicar_reemplazos("backend/db.py") and ok
    ok = aplicar_reemplazos("backend/app.py") and ok
    ok = aplicar_reemplazos("frontend/index.html") and ok

    if not ok:
        print()
        print("Algo no se pudo aplicar automaticamente. Avisale a Claude que mensaje salio, sin correr git add/commit todavia.")
        sys.exit(1)

    print()
    print("Todo listo. Ahora corre:")
    print("   git add backend/app.py backend/db.py backend/pdfs_cotizaciones.py frontend/index.html")
    print('   git commit -m "Cotizador: descuento y nota por articulo, enviar por WhatsApp"')
    print("   git push")


if __name__ == "__main__":
    main()
