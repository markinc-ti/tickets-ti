# -*- coding: utf-8 -*-
"""
Agrega el Cotizador dentro de Checador de precio:
- Pestanas "Cotizador" y "Cotizaciones realizadas".
- Jalar cotizacion/pedido/venta de Microsip por folio (cliente + articulos
  con precio de lista).
- Editar: quitar articulos, agregar articulos de Microsip o manuales.
- Guardar en la app (no se escribe de vuelta a Microsip).
- Buscar/crear cliente igual que en Reparaciones (F4 / lupa).

Uso: colocalo en la carpeta del repo (junto a backend/ y frontend/) y corre:
    py fix_cotizador.py
"""
import sys

ARCHIVOS = {'backend/db.py': [['ALTER TABLE empresas ADD COLUMN IF NOT EXISTS locationiq_api_key TEXT;\n    """)', 'ALTER TABLE empresas ADD COLUMN IF NOT EXISTS locationiq_api_key TEXT;\n\n        -- Cotizador (dentro de Checador de precio): cotizaciones que se\n        -- pueden jalar de Microsip y editar (agregar/quitar artículos, mezclar\n        -- artículos de Microsip con artículos manuales), guardadas en la app\n        -- (no se escriben de vuelta a Microsip).\n        CREATE TABLE IF NOT EXISTS cotizaciones (\n            id SERIAL PRIMARY KEY,\n            empresa_id INTEGER NOT NULL REFERENCES empresas(id),\n            folio TEXT NOT NULL,\n            cliente_nombre TEXT NOT NULL,\n            cliente_direccion TEXT,\n            cliente_telefono TEXT,\n            folio_microsip_origen TEXT,\n            notas TEXT,\n            creado_por_id INTEGER REFERENCES users(id),\n            creado_en TEXT NOT NULL,\n            actualizado_en TEXT NOT NULL\n        );\n\n        CREATE TABLE IF NOT EXISTS cotizacion_items (\n            id SERIAL PRIMARY KEY,\n            cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id) ON DELETE CASCADE,\n            articulo_id INTEGER,\n            clave TEXT,\n            nombre TEXT NOT NULL,\n            cantidad NUMERIC NOT NULL DEFAULT 1,\n            precio_unitario NUMERIC NOT NULL DEFAULT 0,\n            orden INTEGER NOT NULL DEFAULT 0\n        );\n    """)'], ['def eliminar_metrica_marketing(empresa_id, metrica_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("DELETE FROM metricas_marketing WHERE id = %s AND empresa_id = %s", (metrica_id, empresa_id))\n    eliminado = cur.rowcount > 0\n    conn.commit()\n    cur.close(); conn.close()\n    return eliminado\n', 'def eliminar_metrica_marketing(empresa_id, metrica_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("DELETE FROM metricas_marketing WHERE id = %s AND empresa_id = %s", (metrica_id, empresa_id))\n    eliminado = cur.rowcount > 0\n    conn.commit()\n    cur.close(); conn.close()\n    return eliminado\n\n\n# =============================================================================\n# COTIZADOR (dentro de Checador de precio): cotizaciones jaladas de Microsip\n# o creadas desde cero, editables (agregar/quitar artículos de Microsip o\n# manuales), guardadas en la app.\n# =============================================================================\n\ndef _next_folio_cotizacion(cur, empresa_id):\n    cur.execute("SELECT folio FROM cotizaciones WHERE empresa_id = %s", (empresa_id,))\n    maximo = 0\n    for row in cur.fetchall():\n        try:\n            numero = int(row["folio"].split("-")[-1])\n            maximo = max(maximo, numero)\n        except (ValueError, AttributeError, IndexError, TypeError):\n            continue\n    return f"COT-{maximo + 1:04d}"\n\n\ndef _guardar_items_cotizacion(cur, cotizacion_id, items):\n    cur.execute("DELETE FROM cotizacion_items WHERE cotizacion_id = %s", (cotizacion_id,))\n    for i, item in enumerate(items):\n        cur.execute("""\n            INSERT INTO cotizacion_items (cotizacion_id, articulo_id, clave, nombre, cantidad, precio_unitario, orden)\n            VALUES (%s, %s, %s, %s, %s, %s, %s)\n        """, (cotizacion_id, item.get("articulo_id"), item.get("clave"), item["nombre"],\n              item["cantidad"], item["precio_unitario"], i))\n\n\ndef _enriquecer_cotizacion(cur, cotizacion):\n    cur.execute("""\n        SELECT id, articulo_id, clave, nombre, cantidad, precio_unitario\n        FROM cotizacion_items WHERE cotizacion_id = %s ORDER BY orden\n    """, (cotizacion["id"],))\n    cotizacion["items"] = [dict(r) for r in cur.fetchall()]\n    cotizacion["total"] = sum(float(i["cantidad"]) * float(i["precio_unitario"]) for i in cotizacion["items"])\n    return cotizacion\n\n\ndef crear_cotizacion(empresa_id, creado_por_id, cliente_nombre, cliente_direccion, cliente_telefono,\n                      folio_microsip_origen, notas, items):\n    conn = get_connection()\n    cur = conn.cursor()\n    ahora_iso = ahora().isoformat(timespec="seconds")\n    folio = _next_folio_cotizacion(cur, empresa_id)\n    cur.execute("""\n        INSERT INTO cotizaciones (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n                                   folio_microsip_origen, notas, creado_por_id, creado_en, actualizado_en)\n        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)\n        RETURNING id\n    """, (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n          folio_microsip_origen, notas, creado_por_id, ahora_iso, ahora_iso))\n    cotizacion_id = cur.fetchone()["id"]\n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado\n\n\ndef listar_cotizaciones(empresa_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.empresa_id = %s ORDER BY c.id DESC\n    """, (empresa_id,))\n    cotizaciones = [dict(r) for r in cur.fetchall()]\n    for c in cotizaciones:\n        _enriquecer_cotizacion(cur, c)\n    cur.close(); conn.close()\n    return cotizaciones\n\n\ndef obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=None):\n    conn, cur = _conn_cur if _conn_cur else (get_connection(), None)\n    if cur is None:\n        cur = conn.cursor()\n    cur.execute("""\n        SELECT c.*, u.nombre_completo AS creado_por_nombre\n        FROM cotizaciones c\n        LEFT JOIN users u ON u.id = c.creado_por_id\n        WHERE c.id = %s AND c.empresa_id = %s\n    """, (cotizacion_id, empresa_id))\n    row = cur.fetchone()\n    resultado = _enriquecer_cotizacion(cur, dict(row)) if row else None\n    if not _conn_cur:\n        cur.close(); conn.close()\n    return resultado\n\n\ndef actualizar_cotizacion(empresa_id, cotizacion_id, cliente_nombre, cliente_direccion, cliente_telefono, notas, items):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("SELECT id FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    if not cur.fetchone():\n        cur.close(); conn.close()\n        return None\n    cur.execute("""\n        UPDATE cotizaciones SET cliente_nombre = %s, cliente_direccion = %s, cliente_telefono = %s,\n                                 notas = %s, actualizado_en = %s\n        WHERE id = %s\n    """, (cliente_nombre, cliente_direccion, cliente_telefono, notas,\n          ahora().isoformat(timespec="seconds"), cotizacion_id))\n    _guardar_items_cotizacion(cur, cotizacion_id, items)\n    conn.commit()\n    resultado = obtener_cotizacion(empresa_id, cotizacion_id, _conn_cur=(conn, cur))\n    cur.close(); conn.close()\n    return resultado\n\n\ndef eliminar_cotizacion(empresa_id, cotizacion_id):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("DELETE FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    eliminado = cur.rowcount > 0\n    conn.commit()\n    cur.close(); conn.close()\n    return eliminado\n']], 'backend/app.py': [['@app.get("/api/checador-precio/buscar")\ndef api_checador_precio_buscar(q: str, usuario: dict = Depends(requiere_ver_checador_precio)):\n    """Búsqueda de productos por nombre — botón de lupa cuando la\n    clave/código de barras no dio resultado."""\n    config = _config_microsip_o_error(usuario)\n    try:\n        return microsip.buscar_productos_por_nombre(config, q)\n    except Exception as e:\n        raise HTTPException(status_code=400, detail=f"Error consultando Microsip: {e}")\n\n\n@app.get("/api/microsip/tablas")', '@app.get("/api/checador-precio/buscar")\ndef api_checador_precio_buscar(q: str, usuario: dict = Depends(requiere_ver_checador_precio)):\n    """Búsqueda de productos por nombre — botón de lupa cuando la\n    clave/código de barras no dio resultado."""\n    config = _config_microsip_o_error(usuario)\n    try:\n        return microsip.buscar_productos_por_nombre(config, q)\n    except Exception as e:\n        raise HTTPException(status_code=400, detail=f"Error consultando Microsip: {e}")\n\n\n# ---- Cotizador (dentro de Checador de precio) ----\n\nclass CotizacionItemIn(BaseModel):\n    articulo_id: Optional[int] = None\n    clave: Optional[str] = None\n    nombre: str = Field(min_length=1)\n    cantidad: float = Field(gt=0)\n    precio_unitario: float = Field(ge=0)\n\n\nclass CotizacionIn(BaseModel):\n    cliente_nombre: str = Field(min_length=1)\n    cliente_direccion: Optional[str] = None\n    cliente_telefono: Optional[str] = None\n    folio_microsip_origen: Optional[str] = None\n    notas: Optional[str] = None\n    items: List[CotizacionItemIn] = Field(default_factory=list)\n\n\n@app.get("/api/cotizaciones/microsip/{folio}")\ndef api_cotizador_buscar_microsip(folio: str, usuario: dict = Depends(requiere_ver_checador_precio)):\n    """Jala un documento de Microsip (cotización, pedido, o venta) con\n    precio de lista por artículo, para empezar a armar la cotización."""\n    config = _config_microsip_o_error(usuario)\n    try:\n        return microsip.buscar_cotizacion_microsip(config, folio)\n    except ValueError as e:\n        raise HTTPException(status_code=404, detail=str(e))\n    except Exception as e:\n        raise HTTPException(status_code=400, detail=f"Error consultando Microsip: {e}")\n\n\n@app.get("/api/cotizaciones")\ndef api_listar_cotizaciones(usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.listar_cotizaciones(usuario["empresa_id"])\n\n\n@app.get("/api/cotizaciones/{cotizacion_id}")\ndef api_obtener_cotizacion(cotizacion_id: int, usuario: dict = Depends(requiere_ver_checador_precio)):\n    cotizacion = db.obtener_cotizacion(usuario["empresa_id"], cotizacion_id)\n    if not cotizacion:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return cotizacion\n\n\n@app.post("/api/cotizaciones")\ndef api_crear_cotizacion(payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.crear_cotizacion(\n        usuario["empresa_id"], usuario["id"], payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.folio_microsip_origen, payload.notas,\n        [item.model_dump() for item in payload.items],\n    )\n\n\n@app.put("/api/cotizaciones/{cotizacion_id}")\ndef api_actualizar_cotizacion(cotizacion_id: int, payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    resultado = db.actualizar_cotizacion(\n        usuario["empresa_id"], cotizacion_id, payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.notas, [item.model_dump() for item in payload.items],\n    )\n    if not resultado:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return resultado\n\n\n@app.delete("/api/cotizaciones/{cotizacion_id}")\ndef api_eliminar_cotizacion(cotizacion_id: int, usuario: dict = Depends(requiere_ver_checador_precio)):\n    if not db.eliminar_cotizacion(usuario["empresa_id"], cotizacion_id):\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return {"ok": True}\n\n\n@app.get("/api/microsip/tablas")']], 'frontend/index.html': [['<div id="checadorPrecioScreen" style="display:none;">\n  <header>\n    <div class="brand">\n      <span class="chip-id">TI//$$$</span>\n      <div>\n        <h1>Checador de precio</h1>\n        <div class="subtitle">costo y existencia en tiempo real</div>\n      </div>\n    </div>\n    <div class="header-actions">\n      <div class="whoami" id="whoamiChecadorPrecio"></div>\n      <button class="secondary" onclick="volverATableroDesdeChecadorPrecio()">← Menú principal</button>\n      <button class="secondary" onclick="cerrarSesionConConfirmacion()">Salir</button>\n    </div>\n  </header>\n  <main>\n    <div id="checadorPrecioContenido" style="max-width:700px; margin:0 auto;"></div>\n  </main>\n</div>\n\n', '<div id="checadorPrecioScreen" style="display:none;">\n  <header>\n    <div class="brand">\n      <span class="chip-id">TI//$$$</span>\n      <div>\n        <h1>Checador de precio</h1>\n        <div class="subtitle">costo y existencia en tiempo real</div>\n      </div>\n    </div>\n    <div class="header-actions">\n      <div class="whoami" id="whoamiChecadorPrecio"></div>\n      <button class="secondary" onclick="volverATableroDesdeChecadorPrecio()">← Menú principal</button>\n      <button class="secondary" onclick="cerrarSesionConConfirmacion()">Salir</button>\n    </div>\n  </header>\n  <main>\n    <div class="admin-tabs" style="max-width:900px; margin:0 auto 18px;">\n      <button class="admin-tab" id="tabCPBuscar" onclick="cambiarChecadorPrecioTab(\'buscar\')">💲 Buscar precio</button>\n      <button class="admin-tab" id="tabCPCotizador" onclick="cambiarChecadorPrecioTab(\'cotizador\')">🧾 Cotizador</button>\n      <button class="admin-tab" id="tabCPRealizadas" onclick="cambiarChecadorPrecioTab(\'realizadas\')">📋 Cotizaciones realizadas</button>\n    </div>\n    <div id="checadorPrecioContenido" style="max-width:900px; margin:0 auto;"></div>\n  </main>\n</div>\n\n'], ["async function abrirChecadorPrecio() {\n  document.getElementById('app').style.display = 'none';\n  document.getElementById('menuScreen').style.display = 'none';\n  document.getElementById('equiposScreen').style.display = 'none';\n  document.getElementById('proyectosScreen').style.display = 'none';\n  document.getElementById('marketingScreen').style.display = 'none';\n  document.getElementById('comprasScreen').style.display = 'none';\n  document.getElementById('rhScreen').style.display = 'none';\n  document.getElementById('dashboardScreen').style.display = 'none';\n  document.getElementById('superadminScreen').style.display = 'none';\n  document.getElementById('reparacionesScreen').style.display = 'none';\n  document.getElementById('entregasScreen').style.display = 'none';\n  document.getElementById('checadorPrecioScreen').style.display = 'block';\n  document.getElementById('whoamiChecadorPrecio').innerHTML = `<b>${escapeHtml(SESION.usuario.nombre)}</b><br>${NOMBRES_ROL[SESION.usuario.rol] || SESION.usuario.rol}`;\n  renderFormularioChecadorPrecio();\n}\n", "async function abrirChecadorPrecio() {\n  document.getElementById('app').style.display = 'none';\n  document.getElementById('menuScreen').style.display = 'none';\n  document.getElementById('equiposScreen').style.display = 'none';\n  document.getElementById('proyectosScreen').style.display = 'none';\n  document.getElementById('marketingScreen').style.display = 'none';\n  document.getElementById('comprasScreen').style.display = 'none';\n  document.getElementById('rhScreen').style.display = 'none';\n  document.getElementById('dashboardScreen').style.display = 'none';\n  document.getElementById('superadminScreen').style.display = 'none';\n  document.getElementById('reparacionesScreen').style.display = 'none';\n  document.getElementById('entregasScreen').style.display = 'none';\n  document.getElementById('checadorPrecioScreen').style.display = 'block';\n  document.getElementById('whoamiChecadorPrecio').innerHTML = `<b>${escapeHtml(SESION.usuario.nombre)}</b><br>${NOMBRES_ROL[SESION.usuario.rol] || SESION.usuario.rol}`;\n  await cambiarChecadorPrecioTab(CP_TAB);\n}\n\nlet CP_TAB = 'buscar';\n\nasync function cambiarChecadorPrecioTab(tab) {\n  detenerEscaneoChecadorPrecio();\n  CP_TAB = tab;\n  document.getElementById('tabCPBuscar').classList.toggle('active', tab === 'buscar');\n  document.getElementById('tabCPCotizador').classList.toggle('active', tab === 'cotizador');\n  document.getElementById('tabCPRealizadas').classList.toggle('active', tab === 'realizadas');\n  if (tab === 'buscar') {\n    renderFormularioChecadorPrecio();\n  } else if (tab === 'cotizador') {\n    renderCotizadorNueva();\n  } else {\n    await renderCotizacionesRealizadas();\n  }\n}\n\n"], ["function detenerEscaneoChecadorPrecio() {\n  if (ZXING_LECTOR) {\n    try { ZXING_LECTOR.reset(); } catch (e) {}\n    ZXING_LECTOR = null;\n  }\n  const div = document.getElementById('escanerOverlay');\n  if (div) div.remove();\n}\n", 'function detenerEscaneoChecadorPrecio() {\n  if (ZXING_LECTOR) {\n    try { ZXING_LECTOR.reset(); } catch (e) {}\n    ZXING_LECTOR = null;\n  }\n  const div = document.getElementById(\'escanerOverlay\');\n  if (div) div.remove();\n}\n\n// ---- Cotizador (dentro de Checador de precio) ----\n\nlet COT_ACTUAL = null; // { id: null|number, folio, cliente_nombre, cliente_direccion, cliente_telefono, folio_microsip_origen, notas, items: [...] }\nlet COT_ADD_RESULTADOS = [];\n\nfunction cot_nuevaVacia() {\n  return { id: null, folio: null, cliente_nombre: \'\', cliente_direccion: \'\', cliente_telefono: \'\', folio_microsip_origen: null, notas: \'\', items: [] };\n}\n\nfunction renderCotizadorNueva() {\n  COT_ACTUAL = cot_nuevaVacia();\n  renderCotizadorForm();\n}\n\nfunction cot_fmt(n) {\n  n = Number(n) || 0;\n  return n.toLocaleString(\'es-MX\', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n}\n\nfunction cot_total() {\n  return COT_ACTUAL.items.reduce((suma, it) => suma + (Number(it.cantidad) || 0) * (Number(it.precio_unitario) || 0), 0);\n}\n\n// Antes de cualquier acción que vuelva a dibujar el formulario (agregar/quitar\n// artículo, guardar), se leen los inputs que el usuario ya haya tocado —\n// para no perder ediciones a medio hacer en otras filas.\nfunction cot_sincronizarDesdeInputs() {\n  if (!COT_ACTUAL) return;\n  const nombreEl = document.getElementById(\'cot_cliente_nombre\');\n  const dirEl = document.getElementById(\'cot_cliente_direccion\');\n  const telEl = document.getElementById(\'cot_cliente_telefono\');\n  const notasEl = document.getElementById(\'cot_notas\');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;\n  COT_ACTUAL.items.forEach((it, i) => {\n    const n = document.getElementById(`cot_item_nombre_${i}`);\n    const c = document.getElementById(`cot_item_cant_${i}`);\n    const p = document.getElementById(`cot_item_precio_${i}`);\n    if (n) it.nombre = n.value;\n    if (c) it.cantidad = parseFloat(c.value) || 0;\n    if (p) it.precio_unitario = parseFloat(p.value) || 0;\n  });\n}\n\nfunction cot_actualizarTotalEnPantalla() {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.forEach((it, i) => {\n    const sub = document.getElementById(`cot_item_subtotal_${i}`);\n    if (sub) sub.textContent = \'$\' + cot_fmt((Number(it.cantidad) || 0) * (Number(it.precio_unitario) || 0));\n  });\n  const totalEl = document.getElementById(\'cot_total\');\n  if (totalEl) totalEl.textContent = \'$\' + cot_fmt(cot_total());\n}\n\nfunction renderCotizadorForm() {\n  const c = COT_ACTUAL;\n  document.getElementById(\'checadorPrecioContenido\').innerHTML = `\n    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px;">\n      <h3 style="margin:0;">${c.id ? `Editando ${escapeHtml(c.folio)}` : \'Nueva cotización\'}</h3>\n      ${c.id ? \'<button class="secondary" onclick="renderCotizadorNueva()">+ Nueva cotización</button>\' : \'\'}\n    </div>\n\n    ${!c.id ? `\n      <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:14px; margin-bottom:16px;">\n        <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Jalar de Microsip (opcional)</label>\n        <p style="font-size:12px; color:var(--muted); margin:4px 0 8px;">Trae cliente y artículos de una cotización, pedido, o venta ya hecha en Microsip — todo queda editable después.</p>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_folio_microsip" autocomplete="off" placeholder="Folio en Microsip (ej. AMI69)" style="flex:1;"\n                 onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarMicrosipUI(this.nextElementSibling);}" />\n          <button type="button" class="primary" onclick="cot_buscarMicrosipUI(this)">Buscar</button>\n        </div>\n        <div id="cot_microsip_error" style="margin-top:6px;"></div>\n      </div>\n      <div style="text-align:center; font-size:11px; color:var(--muted); margin:-6px 0 16px;">— o arma una cotización desde cero abajo —</div>\n    ` : \'\'}\n\n    <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Cliente</label>\n    <div class="row-2" style="margin-top:6px;">\n      <div class="field"><label>Nombre * <span style="color:var(--muted); font-weight:400; text-transform:none;">(F4 busca en Microsip)</span></label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_nombre" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_nombre || \'\')}"\n                 onkeydown="if(event.key===\'F4\'){event.preventDefault(); cot_abrirBuscarClienteMicrosip(\'nombre\');}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por nombre" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'nombre\')">🔍</button>\n        </div>\n      </div>\n      <div class="field"><label>Teléfono</label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_telefono" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_telefono || \'\')}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por teléfono" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'telefono\')">🔍</button>\n        </div>\n      </div>\n    </div>\n    <div class="field"><label>Dirección</label><input id="cot_cliente_direccion" autocomplete="off" value="${escapeHtml(c.cliente_direccion || \'\')}" /></div>\n\n    <label style="font-size:11px; color:var(--muted); text-transform:uppercase; display:block; margin-top:18px;">Artículos</label>\n    <table class="users" style="margin-top:6px;">\n      <thead><tr><th>Artículo</th><th style="width:90px;">Cantidad</th><th style="width:120px;">Precio unit.</th><th style="width:100px;">Subtotal</th><th></th></tr></thead>\n      <tbody>\n        ${c.items.map((it, i) => `\n          <tr>\n            <td>\n              <input id="cot_item_nombre_${i}" value="${escapeHtml(it.nombre)}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" />\n              ${it.clave ? `<div style="font-size:10px; color:var(--muted); margin-top:2px;">Microsip: ${escapeHtml(it.clave)}</div>` : \'\'}\n            </td>\n            <td><input id="cot_item_cant_${i}" type="number" step="any" min="0" value="${it.cantidad}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_precio_${i}" type="number" step="0.01" min="0" value="${it.precio_unitario}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td id="cot_item_subtotal_${i}">$${cot_fmt((Number(it.cantidad)||0) * (Number(it.precio_unitario)||0))}</td>\n            <td><button type="button" class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_quitarItem(${i})">✕</button></td>\n          </tr>\n        `).join(\'\') || `<tr><td colspan="5" class="empty-col">— sin artículos todavía —</td></tr>`}\n      </tbody>\n    </table>\n    <p style="text-align:right; font-size:16px; margin-top:8px;"><b>Total: <span id="cot_total">$${cot_fmt(cot_total())}</span></b></p>\n\n    <div style="display:grid; grid-template-columns: 1fr 1fr; gap:16px; margin-top:6px;">\n      <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n        <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo de Microsip</label>\n        <div style="display:flex; gap:6px; margin-top:8px;">\n          <input id="cot_add_clave" autocomplete="off" placeholder="Clave / código" style="flex:1;"\n                 onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarClaveParaAgregar(this.nextElementSibling);}" />\n          <button type="button" class="secondary" onclick="cot_buscarClaveParaAgregar(this)">Agregar</button>\n        </div>\n        <div style="display:flex; gap:6px; margin-top:6px;">\n          <input id="cot_add_nombre" autocomplete="off" placeholder="O busca por nombre" style="flex:1;"\n                 onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarNombreParaAgregar();}" />\n          <button type="button" class="secondary" onclick="cot_buscarNombreParaAgregar()">🔍</button>\n        </div>\n        <div id="cot_add_error" style="margin-top:6px;"></div>\n        <div id="cot_add_resultados" style="margin-top:6px;"></div>\n      </div>\n      <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n        <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo manual</label>\n        <div class="field" style="margin-top:8px;"><input id="cot_manual_nombre" autocomplete="off" placeholder="Nombre del artículo o servicio" /></div>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_manual_cantidad" type="number" step="any" min="0" value="1" placeholder="Cantidad" style="width:90px;" />\n          <input id="cot_manual_precio" type="number" step="0.01" min="0" placeholder="Precio unit." style="flex:1;" />\n          <button type="button" class="secondary" onclick="cot_agregarItemManual()">Agregar</button>\n        </div>\n      </div>\n    </div>\n\n    <div class="field" style="margin-top:16px;"><label>Notas (opcional)</label><textarea id="cot_notas">${escapeHtml(c.notas || \'\')}</textarea></div>\n\n    <div id="cot_guardar_error" style="margin-top:6px;"></div>\n    <button class="primary" style="width:100%; margin-top:10px;" onclick="cot_guardarUI(this)">${c.id ? \'Guardar cambios\' : \'Guardar cotización\'}</button>\n  `;\n}\n\nasync function cot_buscarMicrosipUI(boton) {\n  const folio = document.getElementById(\'cot_folio_microsip\').value.trim();\n  const errDiv = document.getElementById(\'cot_microsip_error\');\n  if (!folio) return;\n  errDiv.innerHTML = \'\';\n  await conBloqueoDeBoton(boton, async () => {\n    try {\n      const datos = await api(`/api/cotizaciones/microsip/${encodeURIComponent(folio)}`);\n      COT_ACTUAL.cliente_nombre = datos.cliente_nombre || \'\';\n      COT_ACTUAL.cliente_direccion = datos.cliente_direccion || \'\';\n      COT_ACTUAL.cliente_telefono = datos.cliente_telefono || \'\';\n      COT_ACTUAL.folio_microsip_origen = datos.folio_encontrado;\n      COT_ACTUAL.items = datos.items.map(it => ({\n        articulo_id: it.articulo_id, clave: it.clave, nombre: it.nombre,\n        cantidad: it.cantidad, precio_unitario: it.precio_unitario,\n      }));\n      renderCotizadorForm();\n      mostrarExito(`Se cargó el folio ${datos.folio_encontrado} de Microsip — revisa y ajusta antes de guardar.`);\n    } catch (e) {\n      errDiv.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n    }\n  });\n}\n\nfunction cot_quitarItem(indice) {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.splice(indice, 1);\n  renderCotizadorForm();\n}\n\nasync function cot_buscarClaveParaAgregar(boton) {\n  const clave = document.getElementById(\'cot_add_clave\').value.trim();\n  const errDiv = document.getElementById(\'cot_add_error\');\n  document.getElementById(\'cot_add_resultados\').innerHTML = \'\';\n  errDiv.innerHTML = \'\';\n  if (!clave) return;\n  await conBloqueoDeBoton(boton, async () => {\n    try {\n      const datos = await api(`/api/checador-precio/clave/${encodeURIComponent(clave)}`);\n      cot_agregarItemDesdeArticulo(datos);\n      document.getElementById(\'cot_add_clave\').value = \'\';\n    } catch (e) {\n      errDiv.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)} — prueba buscarlo por nombre.</p>`;\n    }\n  });\n}\n\nasync function cot_buscarNombreParaAgregar() {\n  const texto = document.getElementById(\'cot_add_nombre\').value.trim();\n  const cont = document.getElementById(\'cot_add_resultados\');\n  document.getElementById(\'cot_add_error\').innerHTML = \'\';\n  if (!texto) return;\n  cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">Buscando…</p>\';\n  try {\n    COT_ADD_RESULTADOS = await api(`/api/checador-precio/buscar?q=${encodeURIComponent(texto)}`);\n    if (!COT_ADD_RESULTADOS.length) {\n      cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">No se encontró ningún producto con ese nombre.</p>\';\n      return;\n    }\n    cont.innerHTML = `\n      <ul class="lista-simple">\n        ${COT_ADD_RESULTADOS.map((r, i) => `\n          <li style="cursor:pointer; padding:6px 0; border-bottom:1px solid rgba(155,157,159,0.15); font-size:13px;" onclick="cot_seleccionarResultadoParaAgregar(${i})">\n            <b>${escapeHtml(r.nombre)}</b>${r.clave ? ` <span style="font-size:11px; color:var(--muted);">(${escapeHtml(r.clave)})</span>` : \'\'}\n          </li>\n        `).join(\'\')}\n      </ul>\n    `;\n  } catch (e) {\n    cont.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n  }\n}\n\nasync function cot_seleccionarResultadoParaAgregar(indice) {\n  const producto = COT_ADD_RESULTADOS[indice];\n  if (!producto) return;\n  try {\n    const datos = await api(`/api/checador-precio/articulo/${producto.articulo_id}`);\n    cot_agregarItemDesdeArticulo(datos);\n    document.getElementById(\'cot_add_nombre\').value = \'\';\n    document.getElementById(\'cot_add_resultados\').innerHTML = \'\';\n  } catch (e) {\n    document.getElementById(\'cot_add_error\').innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n  }\n}\n\nfunction cot_agregarItemDesdeArticulo(datos) {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({\n    articulo_id: datos.articulo_id,\n    clave: (datos.claves && datos.claves[0]) || null,\n    nombre: datos.nombre,\n    cantidad: 1,\n    precio_unitario: datos.precio_con_impuesto || 0,\n  });\n  renderCotizadorForm();\n}\n\nfunction cot_agregarItemManual() {\n  const nombre = document.getElementById(\'cot_manual_nombre\').value.trim();\n  const cantidad = parseFloat(document.getElementById(\'cot_manual_cantidad\').value) || 0;\n  const precio = parseFloat(document.getElementById(\'cot_manual_precio\').value) || 0;\n  if (!nombre) { document.getElementById(\'cot_manual_nombre\').focus(); return; }\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.push({ articulo_id: null, clave: null, nombre, cantidad: cantidad || 1, precio_unitario: precio });\n  renderCotizadorForm();\n}\n\nasync function cot_guardarUI(boton) {\n  cot_sincronizarDesdeInputs();\n  const errDiv = document.getElementById(\'cot_guardar_error\');\n  errDiv.innerHTML = \'\';\n  if (!COT_ACTUAL.cliente_nombre.trim()) {\n    errDiv.innerHTML = \'<p style="font-size:12px; color:var(--copper);">Falta el nombre del cliente.</p>\';\n    return;\n  }\n  if (!COT_ACTUAL.items.length) {\n    errDiv.innerHTML = \'<p style="font-size:12px; color:var(--copper);">Agrega al menos un artículo.</p>\';\n    return;\n  }\n  const payload = {\n    cliente_nombre: COT_ACTUAL.cliente_nombre.trim(),\n    cliente_direccion: COT_ACTUAL.cliente_direccion || null,\n    cliente_telefono: COT_ACTUAL.cliente_telefono || null,\n    folio_microsip_origen: COT_ACTUAL.folio_microsip_origen || null,\n    notas: COT_ACTUAL.notas || null,\n    items: COT_ACTUAL.items.map(it => ({\n      articulo_id: it.articulo_id || null, clave: it.clave || null, nombre: it.nombre,\n      cantidad: Number(it.cantidad) || 0, precio_unitario: Number(it.precio_unitario) || 0,\n    })),\n  };\n  await conBloqueoDeBoton(boton, async () => {\n    try {\n      const guardada = COT_ACTUAL.id\n        ? await api(`/api/cotizaciones/${COT_ACTUAL.id}`, { method: \'PUT\', body: JSON.stringify(payload) })\n        : await api(\'/api/cotizaciones\', { method: \'POST\', body: JSON.stringify(payload) });\n      mostrarExito(`Cotización ${guardada.folio} guardada`);\n      await cambiarChecadorPrecioTab(\'realizadas\');\n    } catch (e) {\n      errDiv.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n    }\n  });\n}\n\n// ---- Buscar cliente en Microsip para el Cotizador (F4 / lupa) ----\n\nlet RESULTADOS_BUSQUEDA_CLIENTE_COT = [];\nlet MODO_BUSQUEDA_CLIENTE_COT = \'nombre\';\n\nfunction cot_abrirBuscarClienteMicrosip(modo) {\n  modo = modo || \'nombre\';\n  MODO_BUSQUEDA_CLIENTE_COT = modo;\n  if (document.getElementById(\'cotBuscarClienteOverlay\')) return;\n  const valorInicial = modo === \'telefono\'\n    ? (document.getElementById(\'cot_cliente_telefono\')?.value || \'\')\n    : (document.getElementById(\'cot_cliente_nombre\')?.value || \'\');\n  const div = document.createElement(\'div\');\n  div.id = \'cotBuscarClienteOverlay\';\n  div.style.cssText = \'position:fixed; inset:0; background:rgba(0,0,0,0.85); z-index:9999; display:flex; align-items:flex-start; justify-content:center; padding:60px 20px;\';\n  div.innerHTML = `\n    <div style="background:var(--substrate); border:1px solid rgba(155,157,159,0.3); border-radius:8px; padding:20px; max-width:480px; width:100%; max-height:75vh; overflow:auto;">\n      <button class="close-btn" onclick="cot_cerrarBuscarClienteMicrosip()">cerrar</button>\n      <h3 style="margin-top:0;">Buscar cliente en Microsip ${modo === \'telefono\' ? \'por teléfono\' : \'por nombre\'}</h3>\n      <p style="font-size:12px; color:var(--muted);">${modo === \'telefono\' ? \'Escribe parte o todo el número.\' : \'Escribe una o varias palabras del nombre.\'} Si no lo encuentras, cierra esta ventana y escríbelo manualmente.</p>\n      <div style="display:flex; gap:6px; margin-top:10px;">\n        <input id="cot_bc_texto" autocomplete="off" placeholder="${modo === \'telefono\' ? \'ej. 2221234567\' : \'ej. juan perez\'}" value="${escapeHtml(valorInicial)}" style="flex:1;" onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_ejecutarBusquedaClienteMicrosip();}" />\n        <button class="secondary" onclick="cot_ejecutarBusquedaClienteMicrosip()">Buscar</button>\n      </div>\n      <div id="cot_bc_resultados" style="margin-top:12px;"></div>\n    </div>\n  `;\n  document.body.appendChild(div);\n  setTimeout(() => { const el = document.getElementById(\'cot_bc_texto\'); el.focus(); el.select(); }, 50);\n}\n\nfunction cot_cerrarBuscarClienteMicrosip() {\n  const div = document.getElementById(\'cotBuscarClienteOverlay\');\n  if (div) div.remove();\n}\n\nasync function cot_ejecutarBusquedaClienteMicrosip() {\n  const texto = document.getElementById(\'cot_bc_texto\').value.trim();\n  const cont = document.getElementById(\'cot_bc_resultados\');\n  if (!texto) { cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">Escribe algo para buscar.</p>\'; return; }\n  cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">Buscando…</p>\';\n  try {\n    RESULTADOS_BUSQUEDA_CLIENTE_COT = await api(`/api/microsip/clientes?q=${encodeURIComponent(texto)}&campo=${MODO_BUSQUEDA_CLIENTE_COT}`);\n    if (!RESULTADOS_BUSQUEDA_CLIENTE_COT.length) {\n      cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">No se encontró ningún cliente con eso en Microsip. Cierra esta ventana y escríbelo manualmente.</p>\';\n      return;\n    }\n    cont.innerHTML = `\n      <ul class="lista-simple">\n        ${RESULTADOS_BUSQUEDA_CLIENTE_COT.map((cl, i) => `\n          <li style="cursor:pointer; padding:8px 0; border-bottom:1px solid rgba(155,157,159,0.15);" onclick="cot_seleccionarClienteMicrosip(${i})">\n            <b>${escapeHtml(cl.nombre)}</b>\n            ${cl.telefono ? `<br><span style="font-size:11px; color:var(--muted);">Tel: ${escapeHtml(cl.telefono)}</span>` : \'\'}\n            ${cl.direccion ? `<br><span style="font-size:11px; color:var(--muted);">${escapeHtml(cl.direccion)}</span>` : \'\'}\n          </li>\n        `).join(\'\')}\n      </ul>\n    `;\n  } catch (e) {\n    cont.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n  }\n}\n\nfunction cot_seleccionarClienteMicrosip(indice) {\n  const cliente = RESULTADOS_BUSQUEDA_CLIENTE_COT[indice];\n  if (!cliente) return;\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.cliente_nombre = cliente.nombre || \'\';\n  if (cliente.telefono) COT_ACTUAL.cliente_telefono = cliente.telefono;\n  if (cliente.direccion) COT_ACTUAL.cliente_direccion = cliente.direccion;\n  cot_cerrarBuscarClienteMicrosip();\n  renderCotizadorForm();\n}\n\n// ---- Cotizaciones realizadas ----\n\nasync function renderCotizacionesRealizadas() {\n  const cont = document.getElementById(\'checadorPrecioContenido\');\n  cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">Cargando…</p>\';\n  const cotizaciones = await api(\'/api/cotizaciones\');\n  cont.innerHTML = `\n    <table class="users">\n      <thead><tr><th>Folio</th><th>Cliente</th><th>Fecha</th><th>Artículos</th><th>Total</th><th></th></tr></thead>\n      <tbody>\n        ${cotizaciones.map(c => `\n          <tr>\n            <td>${escapeHtml(c.folio)}${c.folio_microsip_origen ? `<br><span style="font-size:10px; color:var(--muted);">Microsip: ${escapeHtml(c.folio_microsip_origen)}</span>` : \'\'}</td>\n            <td>${escapeHtml(c.cliente_nombre)}</td>\n            <td>${escapeHtml((c.creado_en || \'\').slice(0, 10))}</td>\n            <td>${c.items.length}</td>\n            <td>$${cot_fmt(c.total)}</td>\n            <td style="white-space:nowrap;">\n              <button class="secondary" style="padding:4px 8px;" onclick="cot_editarCotizacion(${c.id})">Ver / editar</button>\n              <button class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_eliminarCotizacionUI(${c.id}, this)">Eliminar</button>\n            </td>\n          </tr>\n        `).join(\'\') || `<tr><td colspan="6" class="empty-col">— todavía no hay cotizaciones guardadas —</td></tr>`}\n      </tbody>\n    </table>\n  `;\n}\n\nasync function cot_editarCotizacion(id) {\n  const cont = document.getElementById(\'checadorPrecioContenido\');\n  cont.innerHTML = \'<p style="font-size:12px; color:var(--muted);">Cargando…</p>\';\n  try {\n    COT_ACTUAL = await api(`/api/cotizaciones/${id}`);\n    CP_TAB = \'cotizador\';\n    document.getElementById(\'tabCPBuscar\').classList.remove(\'active\');\n    document.getElementById(\'tabCPCotizador\').classList.add(\'active\');\n    document.getElementById(\'tabCPRealizadas\').classList.remove(\'active\');\n    renderCotizadorForm();\n  } catch (e) {\n    cont.innerHTML = `<p style="font-size:12px; color:var(--copper);">${escapeHtml(e.message)}</p>`;\n  }\n}\n\nasync function cot_eliminarCotizacionUI(id, boton) {\n  if (!confirm(\'¿Eliminar esta cotización? No se puede deshacer.\')) return;\n  await conBloqueoDeBoton(boton, async () => {\n    try {\n      await api(`/api/cotizaciones/${id}`, { method: \'DELETE\' });\n      await renderCotizacionesRealizadas();\n    } catch (e) {\n      alert(e.message);\n    }\n  });\n}\n\n']]}

MICROSIP_APPEND = '\n\n# =============================================================================\n# COTIZADOR: jala un documento de Microsip (cotización, pedido, o venta —\n# igual que buscar_pedido, sin filtrar por tipo porque el código de\n# TIPO_DOCTO varía por empresa) con precio de lista por artículo, para\n# poder editarlo dentro de la app sin tocar Microsip.\n# =============================================================================\n\ndef buscar_cotizacion_microsip(config: dict, folio: str):\n    """Busca un documento en Microsip por folio (cotización, pedido, o\n    venta — se acepta cualquiera, igual que buscar_pedido) y regresa\n    cliente + artículos CON PRECIO DE LISTA (reutilizando la misma lógica\n    ya probada del Checador de precio), para armar una cotización editable."""\n    con = _conectar(config)\n    cur = con.cursor()\n\n    m = re.match(r"^([A-Za-z]*)0*(\\d+)$", folio.strip())\n    prefijo, numero = (m.group(1).upper(), m.group(2)) if m else ("", folio.strip())\n    numero_norm = numero.lstrip("0") or "0"\n\n    cur.execute("""\n        SELECT FOLIO, DOCTO_VE_ID, CLIENTE_ID, TIPO_DOCTO\n        FROM DOCTOS_VE\n        WHERE FOLIO STARTING WITH ?\n    """, (prefijo,))\n\n    coincidencias = []\n    for folio_db, docto_id, cli_id, tipo in cur.fetchall():\n        resto = folio_db[len(prefijo):].lstrip("0") or "0"\n        if resto == numero_norm:\n            coincidencias.append((folio_db, docto_id, cli_id, tipo))\n\n    if not coincidencias:\n        cur.execute("SELECT FIRST 8 FOLIO FROM DOCTOS_VE WHERE FOLIO STARTING WITH ?", (prefijo,))\n        ejemplos = [r[0] for r in cur.fetchall()]\n        con.close()\n        if ejemplos:\n            raise ValueError(\n                f"No hay ningún folio \'{prefijo}\' que numéricamente coincida con \'{numero}\'. "\n                f"Folios que sí existen con el prefijo \'{prefijo}\': {\', \'.join(ejemplos)}"\n            )\n        raise ValueError(f"No existe ningún folio en Microsip que empiece con \'{prefijo}\'.")\n\n    mejor = None\n    mejor_num_items = -1\n    for folio_db, docto_id, cli_id, tipo in coincidencias:\n        cur.execute("SELECT COUNT(*) FROM DOCTOS_VE_DET WHERE DOCTO_VE_ID = ?", (docto_id,))\n        num_items = cur.fetchone()[0]\n        if num_items > mejor_num_items or (num_items == mejor_num_items and (mejor is None or docto_id > mejor[1])):\n            mejor_num_items = num_items\n            mejor = (folio_db, docto_id, cli_id, tipo)\n\n    folio_db, docto_id, cliente_id, tipo_docto = mejor\n\n    cur.execute("SELECT NOMBRE FROM CLIENTES WHERE CLIENTE_ID = ?", (cliente_id,))\n    cliente_row = cur.fetchone()\n    cliente_nombre = (cliente_row[0] if cliente_row else "") or ""\n\n    cur.execute("""\n        SELECT NOMBRE_CALLE, NUM_EXTERIOR, NUM_INTERIOR, COLONIA, POBLACION, TELEFONO1\n        FROM DIRS_CLIENTES WHERE CLIENTE_ID = ? ORDER BY ES_DIR_PPAL DESC\n    """, (cliente_id,))\n    dir_row = cur.fetchone()\n    if dir_row:\n        calle, num_ext, num_int, colonia, poblacion, telefono = dir_row\n        partes = [\n            f"{(calle or \'\').strip()} {(num_ext or \'\').strip()}".strip(),\n            (f"Int. {num_int.strip()}" if num_int else None),\n            (colonia or "").strip() or None,\n            (poblacion or "").strip() or None,\n        ]\n        direccion = ", ".join(p for p in partes if p)\n        telefono = (telefono or "").strip()\n    else:\n        direccion, telefono = "", ""\n\n    cur.execute("""\n        SELECT ARTICULO_ID, UNIDADES\n        FROM DOCTOS_VE_DET\n        WHERE DOCTO_VE_ID = ?\n    """, (docto_id,))\n    partidas = cur.fetchall()\n\n    items = []\n    for articulo_id, unidades in partidas:\n        cantidad = float(unidades or 0)\n        if articulo_id:\n            # Se usa la MISMA función ya probada del Checador de precio para\n            # el precio de lista — no se inventa una columna nueva de precio,\n            # se reutiliza lo que ya se confirmó correcto en producción.\n            info = _consultar_producto_por_articulo_id(cur, articulo_id)\n        else:\n            info = None\n        if info:\n            items.append({\n                "articulo_id": articulo_id,\n                "clave": info["claves"][0] if info["claves"] else None,\n                "nombre": info["nombre"],\n                "cantidad": cantidad,\n                "precio_unitario": info["precio_con_impuesto"] or 0,\n            })\n        else:\n            items.append({\n                "articulo_id": None,\n                "clave": None,\n                "nombre": "(artículo sin nombre en Microsip)",\n                "cantidad": cantidad,\n                "precio_unitario": 0,\n            })\n\n    con.close()\n    return {\n        "folio_encontrado": folio_db,\n        "tipo_docto": (tipo_docto or "").strip(),\n        "cliente_nombre": cliente_nombre.strip(),\n        "cliente_direccion": direccion,\n        "cliente_telefono": telefono,\n        "items": items,\n    }\n'


def leer(ruta):
    with open(ruta, "r", encoding="utf-8", newline=None) as f:
        return f.read()


def escribir(ruta, contenido):
    with open(ruta, "w", encoding="utf-8", newline="") as f:
        f.write(contenido)


def aplicar_reemplazos(ruta):
    reemplazos = ARCHIVOS[ruta]
    try:
        contenido = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    cambios = 0
    hubo_error = False
    for viejo, nuevo in reemplazos:
        if nuevo in contenido:
            continue
        if viejo not in contenido:
            print(f"[{ruta}] No encontre un bloque esperado (el archivo pudo haber cambiado). Avisale a Claude.")
            hubo_error = True
            continue
        contenido = contenido.replace(viejo, nuevo, 1)
        cambios += 1
    escribir(ruta, contenido)
    print(f"[{ruta}] {cambios} cambio(s) aplicado(s).")
    return not hubo_error


def aplicar_microsip():
    ruta = "backend/microsip.py"
    try:
        contenido = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    if MICROSIP_APPEND.strip() in contenido:
        print(f"[{ruta}] Ya estaba aplicado. No hace falta nada.")
        return True
    contenido = contenido + MICROSIP_APPEND
    escribir(ruta, contenido)
    print(f"[{ruta}] Funcion agregada al final del archivo.")
    return True


def main():
    ok = True
    ok = aplicar_reemplazos("backend/db.py") and ok
    ok = aplicar_microsip() and ok
    ok = aplicar_reemplazos("backend/app.py") and ok
    ok = aplicar_reemplazos("frontend/index.html") and ok

    if not ok:
        print()
        print("Algo no se pudo aplicar automaticamente. Avisale a Claude que mensaje salio, sin correr git add/commit todavia.")
        sys.exit(1)

    print()
    print("Todo listo. Ahora corre:")
    print("   git add backend/app.py backend/db.py backend/microsip.py frontend/index.html")
    print('   git commit -m "Cotizador dentro de Checador de precio (jalar/editar/guardar cotizaciones de Microsip)"')
    print("   git push")


if __name__ == "__main__":
    main()
