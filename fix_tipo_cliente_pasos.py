# -*- coding: utf-8 -*-
"""
Cotizador: tipo de cliente (Publico en general / Mayoreo / Distribuidor) que
cambia el titulo del PDF, casilla de descuento mas grande, y flujo por pasos
(los articulos quedan bloqueados hasta que se capture el nombre del cliente).

Uso: colocalo en la carpeta del repo (junto a backend/ y frontend/) y corre:
    py fix_tipo_cliente_pasos.py
"""
import sys

ARCHIVOS = {'backend/db.py': [['        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS token_impresion TEXT UNIQUE;\n        ALTER TABLE cotizacion_items ADD COLUMN IF NOT EXISTS descuento_pct NUMERIC NOT NULL DEFAULT 0;', "        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS token_impresion TEXT UNIQUE;\n        ALTER TABLE cotizaciones ADD COLUMN IF NOT EXISTS tipo_cliente TEXT NOT NULL DEFAULT 'publico';\n        ALTER TABLE cotizacion_items ADD COLUMN IF NOT EXISTS descuento_pct NUMERIC NOT NULL DEFAULT 0;"], ['def crear_cotizacion(empresa_id, creado_por_id, cliente_nombre, cliente_direccion, cliente_telefono,\n                      folio_microsip_origen, notas, items):\n    conn = get_connection()\n    cur = conn.cursor()\n    ahora_iso = ahora().isoformat(timespec="seconds")\n    folio = _next_folio_cotizacion(cur, empresa_id)\n    cur.execute("""\n        INSERT INTO cotizaciones (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n                                   folio_microsip_origen, notas, creado_por_id, creado_en, actualizado_en)\n        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)\n        RETURNING id\n    """, (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n          folio_microsip_origen, notas, creado_por_id, ahora_iso, ahora_iso))\n    cotizacion_id = cur.fetchone()["id"]', 'def crear_cotizacion(empresa_id, creado_por_id, cliente_nombre, cliente_direccion, cliente_telefono,\n                      folio_microsip_origen, notas, items, tipo_cliente="publico"):\n    conn = get_connection()\n    cur = conn.cursor()\n    ahora_iso = ahora().isoformat(timespec="seconds")\n    folio = _next_folio_cotizacion(cur, empresa_id)\n    cur.execute("""\n        INSERT INTO cotizaciones (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n                                   folio_microsip_origen, notas, creado_por_id, creado_en, actualizado_en, tipo_cliente)\n        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)\n        RETURNING id\n    """, (empresa_id, folio, cliente_nombre, cliente_direccion, cliente_telefono,\n          folio_microsip_origen, notas, creado_por_id, ahora_iso, ahora_iso, tipo_cliente))\n    cotizacion_id = cur.fetchone()["id"]'], ['def actualizar_cotizacion(empresa_id, cotizacion_id, cliente_nombre, cliente_direccion, cliente_telefono, notas, items):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("SELECT id FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    if not cur.fetchone():\n        cur.close(); conn.close()\n        return None\n    cur.execute("""\n        UPDATE cotizaciones SET cliente_nombre = %s, cliente_direccion = %s, cliente_telefono = %s,\n                                 notas = %s, actualizado_en = %s\n        WHERE id = %s\n    """, (cliente_nombre, cliente_direccion, cliente_telefono, notas,\n          ahora().isoformat(timespec="seconds"), cotizacion_id))', 'def actualizar_cotizacion(empresa_id, cotizacion_id, cliente_nombre, cliente_direccion, cliente_telefono, notas, items, tipo_cliente="publico"):\n    conn = get_connection()\n    cur = conn.cursor()\n    cur.execute("SELECT id FROM cotizaciones WHERE id = %s AND empresa_id = %s", (cotizacion_id, empresa_id))\n    if not cur.fetchone():\n        cur.close(); conn.close()\n        return None\n    cur.execute("""\n        UPDATE cotizaciones SET cliente_nombre = %s, cliente_direccion = %s, cliente_telefono = %s,\n                                 notas = %s, actualizado_en = %s, tipo_cliente = %s\n        WHERE id = %s\n    """, (cliente_nombre, cliente_direccion, cliente_telefono, notas,\n          ahora().isoformat(timespec="seconds"), tipo_cliente, cotizacion_id))\n    ']], 'backend/app.py': [['from typing import Optional, List\n', 'from typing import Optional, List, Literal\n'], ['class CotizacionIn(BaseModel):\n    cliente_nombre: str = Field(min_length=1)\n    cliente_direccion: Optional[str] = None\n    cliente_telefono: Optional[str] = None\n    folio_microsip_origen: Optional[str] = None\n    notas: Optional[str] = None\n    items: List[CotizacionItemIn] = Field(default_factory=list)', 'class CotizacionIn(BaseModel):\n    cliente_nombre: str = Field(min_length=1)\n    cliente_direccion: Optional[str] = None\n    cliente_telefono: Optional[str] = None\n    folio_microsip_origen: Optional[str] = None\n    notas: Optional[str] = None\n    tipo_cliente: Literal["publico", "mayoreo", "distribuidor"] = "publico"\n    items: List[CotizacionItemIn] = Field(default_factory=list)'], ['def api_crear_cotizacion(payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.crear_cotizacion(\n        usuario["empresa_id"], usuario["id"], payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.folio_microsip_origen, payload.notas,\n        [item.model_dump() for item in payload.items],\n    )\n\n\n@app.put("/api/cotizaciones/{cotizacion_id}")\ndef api_actualizar_cotizacion(cotizacion_id: int, payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    resultado = db.actualizar_cotizacion(\n        usuario["empresa_id"], cotizacion_id, payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.notas, [item.model_dump() for item in payload.items],\n    )\n    if not resultado:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return resultado', 'def api_crear_cotizacion(payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    return db.crear_cotizacion(\n        usuario["empresa_id"], usuario["id"], payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.folio_microsip_origen, payload.notas,\n        [item.model_dump() for item in payload.items], payload.tipo_cliente,\n    )\n\n\n@app.put("/api/cotizaciones/{cotizacion_id}")\ndef api_actualizar_cotizacion(cotizacion_id: int, payload: CotizacionIn, usuario: dict = Depends(requiere_ver_checador_precio)):\n    resultado = db.actualizar_cotizacion(\n        usuario["empresa_id"], cotizacion_id, payload.cliente_nombre, payload.cliente_direccion,\n        payload.cliente_telefono, payload.notas, [item.model_dump() for item in payload.items], payload.tipo_cliente,\n    )\n    if not resultado:\n        raise HTTPException(status_code=404, detail="Cotización no encontrada")\n    return resultado']], 'frontend/index.html': [["function cot_nuevaVacia() {\n  return { id: null, folio: null, cliente_nombre: '', cliente_direccion: '', cliente_telefono: '', folio_microsip_origen: null, notas: '', items: [] };\n}", "function cot_nuevaVacia() {\n  return { id: null, folio: null, cliente_nombre: '', cliente_direccion: '', cliente_telefono: '', folio_microsip_origen: null, notas: '', tipo_cliente: 'publico', items: [] };\n}"], ["  const nombreEl = document.getElementById('cot_cliente_nombre');\n  const dirEl = document.getElementById('cot_cliente_direccion');\n  const telEl = document.getElementById('cot_cliente_telefono');\n  const notasEl = document.getElementById('cot_notas');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;", "  const nombreEl = document.getElementById('cot_cliente_nombre');\n  const dirEl = document.getElementById('cot_cliente_direccion');\n  const telEl = document.getElementById('cot_cliente_telefono');\n  const tipoEl = document.getElementById('cot_tipo_cliente');\n  const notasEl = document.getElementById('cot_notas');\n  if (nombreEl) COT_ACTUAL.cliente_nombre = nombreEl.value;\n  if (dirEl) COT_ACTUAL.cliente_direccion = dirEl.value;\n  if (telEl) COT_ACTUAL.cliente_telefono = telEl.value;\n  if (tipoEl) COT_ACTUAL.tipo_cliente = tipoEl.value;\n  if (notasEl) COT_ACTUAL.notas = notasEl.value;"], ["function cot_actualizarTotalEnPantalla() {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.forEach((it, i) => {\n    const sub = document.getElementById(`cot_item_subtotal_${i}`);\n    if (sub) sub.textContent = '$' + cot_fmt(cot_subtotalItem(it));\n  });\n  const totalEl = document.getElementById('cot_total');\n  if (totalEl) totalEl.textContent = '$' + cot_fmt(cot_total());\n}", "function cot_actualizarTotalEnPantalla() {\n  cot_sincronizarDesdeInputs();\n  COT_ACTUAL.items.forEach((it, i) => {\n    const sub = document.getElementById(`cot_item_subtotal_${i}`);\n    if (sub) sub.textContent = '$' + cot_fmt(cot_subtotalItem(it));\n  });\n  const totalEl = document.getElementById('cot_total');\n  if (totalEl) totalEl.textContent = '$' + cot_fmt(cot_total());\n}\n\n// Paso 1 (Cliente) -> Paso 2 (Artículos): en vez de volver a dibujar todo el\n// formulario en cada tecla (lo que perdería el cursor del campo de nombre),\n// solo se muestra/oculta la sección de artículos ya existente en el DOM.\nfunction cot_actualizarDesbloqueoArticulos() {\n  const nombre = document.getElementById('cot_cliente_nombre').value.trim();\n  const seccion = document.getElementById('cot_seccion_articulos');\n  const bloqueado = document.getElementById('cot_articulos_bloqueado');\n  if (seccion) seccion.style.display = nombre ? 'block' : 'none';\n  if (bloqueado) bloqueado.style.display = nombre ? 'none' : 'block';\n}"], ['    <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Cliente</label>\n    <div class="row-2" style="margin-top:6px;">\n      <div class="field"><label>Nombre * <span style="color:var(--muted); font-weight:400; text-transform:none;">(F4 busca en Microsip)</span></label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_nombre" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_nombre || \'\')}"\n                 onkeydown="if(event.key===\'F4\'){event.preventDefault(); cot_abrirBuscarClienteMicrosip(\'nombre\');}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por nombre" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'nombre\')">🔍</button>\n        </div>\n      </div>\n      <div class="field"><label>Teléfono</label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_telefono" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_telefono || \'\')}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por teléfono" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'telefono\')">🔍</button>\n        </div>\n      </div>\n    </div>\n    <div class="field"><label>Dirección</label><input id="cot_cliente_direccion" autocomplete="off" value="${escapeHtml(c.cliente_direccion || \'\')}" /></div>\n\n    <label style="font-size:11px; color:var(--muted); text-transform:uppercase; display:block; margin-top:18px;">Artículos</label>\n    <table class="users" style="margin-top:6px;">\n      <thead><tr><th>Artículo</th><th style="width:80px;">Cantidad</th><th style="width:110px;">Precio unit.</th><th style="width:70px;">Desc. %</th><th style="width:100px;">Subtotal</th><th></th></tr></thead>\n      <tbody>\n        ${c.items.map((it, i) => `\n          <tr>\n            <td>\n              <input id="cot_item_nombre_${i}" value="${escapeHtml(it.nombre)}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" />\n              ${it.clave ? `<div style="font-size:10px; color:var(--muted); margin-top:2px;">Microsip: ${escapeHtml(it.clave)}</div>` : \'\'}\n              <input id="cot_item_nota_${i}" value="${escapeHtml(it.nota || \'\')}" placeholder="Nota de este artículo (opcional, sale en el PDF)" style="width:100%; font-size:11px; margin-top:4px; padding:4px 6px;" />\n            </td>\n            <td><input id="cot_item_cant_${i}" type="number" step="any" min="0" value="${it.cantidad}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_precio_${i}" type="number" step="0.01" min="0" value="${it.precio_unitario}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td><input id="cot_item_desc_${i}" type="number" step="any" min="0" max="100" value="${it.descuento_pct || 0}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n            <td id="cot_item_subtotal_${i}">$${cot_fmt(cot_subtotalItem(it))}</td>\n            <td><button type="button" class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_quitarItem(${i})">✕</button></td>\n          </tr>\n        `).join(\'\') || `<tr><td colspan="6" class="empty-col">— sin artículos todavía —</td></tr>`}\n      </tbody>\n    </table>\n    <p style="text-align:right; font-size:16px; margin-top:8px;"><b>Total: <span id="cot_total">$${cot_fmt(cot_total())}</span></b></p>\n\n    <div style="display:grid; grid-template-columns: 1fr 1fr; gap:16px; margin-top:6px;">\n      <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n        <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo de Microsip</label>\n        <div style="display:flex; gap:6px; margin-top:8px;">\n          <input id="cot_add_clave" autocomplete="off" placeholder="Clave / código" style="flex:1;"\n                 onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarClaveParaAgregar(this.nextElementSibling);}" />\n          <button type="button" class="secondary" onclick="cot_buscarClaveParaAgregar(this)">Agregar</button>\n        </div>\n        <div style="display:flex; gap:6px; margin-top:6px;">\n          <input id="cot_add_nombre" autocomplete="off" placeholder="O busca por nombre" style="flex:1;"\n                 onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarNombreParaAgregar();}" />\n          <button type="button" class="secondary" onclick="cot_buscarNombreParaAgregar()">🔍</button>\n        </div>\n        <div id="cot_add_error" style="margin-top:6px;"></div>\n        <div id="cot_add_resultados" style="margin-top:6px;"></div>\n      </div>\n      <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n        <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo manual</label>\n        <div class="field" style="margin-top:8px;"><input id="cot_manual_nombre" autocomplete="off" placeholder="Nombre del artículo o servicio" /></div>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_manual_cantidad" type="number" step="any" min="0" value="1" placeholder="Cantidad" style="width:90px;" />\n          <input id="cot_manual_precio" type="number" step="0.01" min="0" placeholder="Precio unit." style="flex:1;" />\n          <button type="button" class="secondary" onclick="cot_agregarItemManual()">Agregar</button>\n        </div>\n      </div>\n    </div>\n\n    <div class="field" style="margin-top:16px;"><label>Notas (opcional)</label><textarea id="cot_notas">${escapeHtml(c.notas || \'\')}</textarea></div>\n\n    <div id="cot_guardar_error" style="margin-top:6px;"></div>\n    <button class="primary" style="width:100%; margin-top:10px;" onclick="cot_guardarUI(this)">${c.id ? \'Guardar cambios\' : \'Guardar cotización\'}</button>\n  `;', '    <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Paso 1 · Cliente</label>\n    <div class="row-2" style="margin-top:6px;">\n      <div class="field"><label>Nombre * <span style="color:var(--muted); font-weight:400; text-transform:none;">(F4 busca en Microsip)</span></label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_nombre" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_nombre || \'\')}"\n                 oninput="cot_actualizarDesbloqueoArticulos()"\n                 onkeydown="if(event.key===\'F4\'){event.preventDefault(); cot_abrirBuscarClienteMicrosip(\'nombre\');}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por nombre" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'nombre\')">🔍</button>\n        </div>\n      </div>\n      <div class="field"><label>Teléfono</label>\n        <div style="display:flex; gap:6px;">\n          <input id="cot_cliente_telefono" autocomplete="off" style="flex:1;" value="${escapeHtml(c.cliente_telefono || \'\')}" />\n          <button type="button" class="secondary" title="Buscar en Microsip por teléfono" style="padding:0 12px;" onclick="cot_abrirBuscarClienteMicrosip(\'telefono\')">🔍</button>\n        </div>\n      </div>\n    </div>\n    <div class="row-2">\n      <div class="field"><label>Dirección</label><input id="cot_cliente_direccion" autocomplete="off" value="${escapeHtml(c.cliente_direccion || \'\')}" /></div>\n      <div class="field"><label>Tipo de cliente</label>\n        <select id="cot_tipo_cliente">\n          <option value="publico" ${(!c.tipo_cliente || c.tipo_cliente === \'publico\') ? \'selected\' : \'\'}>Público en general</option>\n          <option value="mayoreo" ${c.tipo_cliente === \'mayoreo\' ? \'selected\' : \'\'}>Mayoreo</option>\n          <option value="distribuidor" ${c.tipo_cliente === \'distribuidor\' ? \'selected\' : \'\'}>Distribuidor</option>\n        </select>\n      </div>\n    </div>\n\n    <div id="cot_articulos_bloqueado" style="display:${(c.cliente_nombre || \'\').trim() ? \'none\' : \'block\'}; text-align:center; padding:24px; margin-top:18px; border:1px dashed rgba(155,157,159,0.4); border-radius:6px; color:var(--muted); font-size:13px;">\n      🔒 Primero captura el nombre del cliente para poder agregar artículos.\n    </div>\n    <div id="cot_seccion_articulos" style="display:${(c.cliente_nombre || \'\').trim() ? \'block\' : \'none\'};">\n      <label style="font-size:11px; color:var(--muted); text-transform:uppercase; display:block; margin-top:18px;">Paso 2 · Artículos</label>\n      <table class="users" style="margin-top:6px;">\n        <thead><tr><th>Artículo</th><th style="width:80px;">Cantidad</th><th style="width:110px;">Precio unit.</th><th style="width:110px;">Desc. %</th><th style="width:100px;">Subtotal</th><th></th></tr></thead>\n        <tbody>\n          ${c.items.map((it, i) => `\n            <tr>\n              <td>\n                <input id="cot_item_nombre_${i}" value="${escapeHtml(it.nombre)}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" />\n                ${it.clave ? `<div style="font-size:10px; color:var(--muted); margin-top:2px;">Microsip: ${escapeHtml(it.clave)}</div>` : \'\'}\n                <input id="cot_item_nota_${i}" value="${escapeHtml(it.nota || \'\')}" placeholder="Nota de este artículo (opcional, sale en el PDF)" style="width:100%; font-size:11px; margin-top:4px; padding:4px 6px;" />\n              </td>\n              <td><input id="cot_item_cant_${i}" type="number" step="any" min="0" value="${it.cantidad}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n              <td><input id="cot_item_precio_${i}" type="number" step="0.01" min="0" value="${it.precio_unitario}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%;" /></td>\n              <td><input id="cot_item_desc_${i}" type="number" step="any" min="0" max="100" value="${it.descuento_pct || 0}" oninput="cot_actualizarTotalEnPantalla()" style="width:100%; min-width:70px; font-size:15px; padding:8px 6px;" /></td>\n              <td id="cot_item_subtotal_${i}">$${cot_fmt(cot_subtotalItem(it))}</td>\n              <td><button type="button" class="secondary" style="padding:4px 8px; color:var(--copper); border-color:var(--copper);" onclick="cot_quitarItem(${i})">✕</button></td>\n            </tr>\n          `).join(\'\') || `<tr><td colspan="6" class="empty-col">— sin artículos todavía —</td></tr>`}\n        </tbody>\n      </table>\n      <p style="text-align:right; font-size:16px; margin-top:8px;"><b>Total: <span id="cot_total">$${cot_fmt(cot_total())}</span></b></p>\n\n      <div style="display:grid; grid-template-columns: 1fr 1fr; gap:16px; margin-top:6px;">\n        <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n          <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo de Microsip</label>\n          <div style="display:flex; gap:6px; margin-top:8px;">\n            <input id="cot_add_clave" autocomplete="off" placeholder="Clave / código" style="flex:1;"\n                   onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarClaveParaAgregar(this.nextElementSibling);}" />\n            <button type="button" class="secondary" onclick="cot_buscarClaveParaAgregar(this)">Agregar</button>\n          </div>\n          <div style="display:flex; gap:6px; margin-top:6px;">\n            <input id="cot_add_nombre" autocomplete="off" placeholder="O busca por nombre" style="flex:1;"\n                   onkeydown="if(event.key===\'Enter\'){event.preventDefault(); cot_buscarNombreParaAgregar();}" />\n            <button type="button" class="secondary" onclick="cot_buscarNombreParaAgregar()">🔍</button>\n          </div>\n          <div id="cot_add_error" style="margin-top:6px;"></div>\n          <div id="cot_add_resultados" style="margin-top:6px;"></div>\n        </div>\n        <div style="border:1px solid rgba(155,157,159,0.3); border-radius:6px; padding:12px;">\n          <label style="font-size:11px; color:var(--muted); text-transform:uppercase;">Agregar artículo manual</label>\n          <div class="field" style="margin-top:8px;"><input id="cot_manual_nombre" autocomplete="off" placeholder="Nombre del artículo o servicio" /></div>\n          <div style="display:flex; gap:6px;">\n            <input id="cot_manual_cantidad" type="number" step="any" min="0" value="1" placeholder="Cantidad" style="width:90px;" />\n            <input id="cot_manual_precio" type="number" step="0.01" min="0" placeholder="Precio unit." style="flex:1;" />\n            <button type="button" class="secondary" onclick="cot_agregarItemManual()">Agregar</button>\n          </div>\n        </div>\n      </div>\n\n      <div class="field" style="margin-top:16px;"><label>Notas (opcional)</label><textarea id="cot_notas">${escapeHtml(c.notas || \'\')}</textarea></div>\n\n      <div id="cot_guardar_error" style="margin-top:6px;"></div>\n      <button class="primary" style="width:100%; margin-top:10px;" onclick="cot_guardarUI(this)">${c.id ? \'Guardar cambios\' : \'Guardar cotización\'}</button>\n    </div>\n  `;']]}

PDFS_COTIZACIONES_COMPLETO = '"""Generación del PDF de una cotización (módulo Cotizador, dentro de\nChecador de precio) — mismo estilo membretado que los documentos de\nReparaciones."""\nfrom io import BytesIO\n\nfrom reportlab.lib import colors\nfrom reportlab.lib.pagesizes import letter\nfrom reportlab.lib.styles import ParagraphStyle\nfrom reportlab.lib.units import cm\nfrom reportlab.platypus import Paragraph, Spacer, Table, TableStyle, HRFlowable\n\nfrom pdfs_reparaciones import (\n    ROJO, GRIS, GRIS_CLARO, NEGRO, _styles, _encabezado_membretado, _pie_pagina,\n    _formatear_fecha, _doc_template,\n)\n\n\ndef _fmt_dinero(n):\n    n = float(n or 0)\n    return f"${n:,.2f}"\n\n\nTITULOS_TIPO_CLIENTE = {\n    "publico": "COTIZACIÓN PÚBLICO EN GENERAL",\n    "mayoreo": "COTIZACIÓN MAYOREO",\n    "distribuidor": "COTIZACIÓN DISTRIBUIDOR",\n}\n\n\ndef generar_cotizacion_pdf(cotizacion, empresa):\n    styles = _styles()\n    elementos = []\n    titulo = TITULOS_TIPO_CLIENTE.get(cotizacion.get("tipo_cliente"), "COTIZACIÓN")\n    _encabezado_membretado(\n        elementos, styles, titulo,\n        folio=cotizacion["folio"],\n        fecha=f"Fecha: {_formatear_fecha(cotizacion.get(\'creado_en\'))}",\n        etiqueta_folio="Folio",\n    )\n\n    elementos.append(Paragraph("Cliente", styles["Seccion"]))\n    datos_cliente = [("Nombre", cotizacion.get("cliente_nombre"))]\n    if cotizacion.get("cliente_telefono"):\n        datos_cliente.append(("Teléfono", cotizacion["cliente_telefono"]))\n    if cotizacion.get("cliente_direccion"):\n        datos_cliente.append(("Dirección", cotizacion["cliente_direccion"]))\n    for etiqueta, valor in datos_cliente:\n        elementos.append(Paragraph(f"<b>{etiqueta}:</b> {valor or \'—\'}", styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 10))\n    elementos.append(Paragraph("Artículos cotizados", styles["Seccion"]))\n\n    estilo_celda = ParagraphStyle("CeldaTabla", parent=styles["Normal"], fontSize=9, leading=12)\n    estilo_celda_num = ParagraphStyle("CeldaTablaNum", parent=estilo_celda, alignment=2)\n    estilo_nota = ParagraphStyle("NotaItem", parent=estilo_celda, fontSize=7.5, textColor=GRIS, leftIndent=2)\n    hay_descuentos = any(float(item.get("descuento_pct") or 0) > 0 for item in cotizacion["items"])\n    encabezado = [\n        Paragraph("<b>Artículo</b>", estilo_celda),\n        Paragraph("<b>Cant.</b>", estilo_celda_num),\n        Paragraph("<b>Precio unit.</b>", estilo_celda_num),\n    ]\n    if hay_descuentos:\n        encabezado.append(Paragraph("<b>Desc.</b>", estilo_celda_num))\n    encabezado.append(Paragraph("<b>Subtotal</b>", estilo_celda_num))\n    filas = [encabezado]\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        nombre = item["nombre"] + (f" <font size=7 color=\'#74767A\'>(clave: {item[\'clave\']})</font>" if item.get("clave") else "")\n        if item.get("nota"):\n            nombre_parrafo = [Paragraph(nombre, estilo_celda), Paragraph(f"Nota: {item[\'nota\']}", estilo_nota)]\n        else:\n            nombre_parrafo = Paragraph(nombre, estilo_celda)\n        fila = [\n            nombre_parrafo,\n            Paragraph(f"{cantidad:g}", estilo_celda_num),\n            Paragraph(_fmt_dinero(precio), estilo_celda_num),\n        ]\n        if hay_descuentos:\n            fila.append(Paragraph(f"{descuento_pct:g}%" if descuento_pct else "—", estilo_celda_num))\n        fila.append(Paragraph(_fmt_dinero(subtotal), estilo_celda_num))\n        filas.append(fila)\n\n    if hay_descuentos:\n        colWidths = [7.2 * cm, 1.6 * cm, 2.4 * cm, 1.7 * cm, 2.6 * cm]\n    else:\n        colWidths = [8.8 * cm, 1.8 * cm, 2.7 * cm, 2.7 * cm]\n    tabla = Table(filas, colWidths=colWidths, repeatRows=1)\n    tabla.setStyle(TableStyle([\n        ("BACKGROUND", (0, 0), (-1, 0), ROJO),\n        ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),\n        ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),\n        ("TOPPADDING", (0, 0), (-1, -1), 6),\n        ("BOTTOMPADDING", (0, 0), (-1, -1), 6),\n        ("ROWBACKGROUNDS", (0, 1), (-1, -1), [colors.white, GRIS_CLARO]),\n        ("LINEBELOW", (0, 0), (-1, -1), 0.4, GRIS),\n    ]))\n    elementos.append(tabla)\n\n    elementos.append(Spacer(1, 6))\n    tabla_total = Table([[\n        Paragraph("<b>TOTAL</b>", ParagraphStyle("TotalEtiqueta", parent=styles["Normal"], fontSize=12, textColor=NEGRO)),\n        Paragraph(f"<b>{_fmt_dinero(total)}</b>", ParagraphStyle("TotalValor", parent=styles["Normal"], fontSize=12, alignment=2, textColor=ROJO)),\n    ]], colWidths=[13.3 * cm, 2.7 * cm])\n    tabla_total.setStyle(TableStyle([\n        ("LINEABOVE", (0, 0), (-1, 0), 1.2, ROJO),\n        ("TOPPADDING", (0, 0), (-1, -1), 8),\n    ]))\n    elementos.append(tabla_total)\n\n    if cotizacion.get("notas"):\n        elementos.append(Spacer(1, 14))\n        elementos.append(Paragraph("Notas", styles["Seccion"]))\n        elementos.append(Paragraph(cotizacion["notas"], styles["Cuerpo"]))\n\n    elementos.append(Spacer(1, 20))\n    elementos.append(HRFlowable(width="100%", thickness=0.8, color=GRIS, spaceBefore=4, spaceAfter=8))\n    elementos.append(Paragraph(\n        "Esta cotización es informativa y no representa una factura. Precios sujetos a cambio sin previo aviso; "\n        "vigencia de 15 días naturales salvo que se indique lo contrario.",\n        ParagraphStyle("Vigencia", parent=styles["Normal"], fontSize=7.5, textColor=GRIS),\n    ))\n\n    buffer = BytesIO()\n    documento = _doc_template(buffer)\n    documento.build(elementos, onFirstPage=_pie_pagina, onLaterPages=_pie_pagina)\n    buffer.seek(0)\n    return buffer.read()\n\n\ndef _escapar_html(texto):\n    return (\n        (texto or "")\n        .replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")\n        .replace(\'"\', "&quot;")\n    )\n\n\ndef _fmt_cant(n):\n    n = float(n or 0)\n    return f"{n:g}"\n\n\ndef generar_html_recibo_termico(cotizacion):\n    """Recibo angosto (58mm) para la impresora térmica Star SM-L200, servido\n    en la ruta pública que la app Star PassPRNT consulta directamente (no\n    lleva sesión ni token de la app — por eso nunca incluye datos sensibles\n    de más, solo lo mismo que ya trae la cotización). Se evitan caracteres\n    tipográficos poco comunes (guion en vez de punto medio, etc.) por si la\n    fuente de la impresora no los trae."""\n    filas = ""\n    total = 0.0\n    for item in cotizacion["items"]:\n        cantidad = float(item["cantidad"])\n        precio = float(item["precio_unitario"])\n        descuento_pct = float(item.get("descuento_pct") or 0)\n        subtotal = cantidad * precio * (1 - descuento_pct / 100)\n        total += subtotal\n        clave = f" ({_escapar_html(item[\'clave\'])})" if item.get("clave") else ""\n        desc = f" (-{descuento_pct:g}%)" if descuento_pct else ""\n        filas += f"""\n          <tr>\n            <td style="text-align:left; padding:3px 0;">{_escapar_html(item[\'nombre\'])}{clave}<br>{_fmt_cant(cantidad)} x {_fmt_dinero(precio)}{desc}</td>\n            <td style="text-align:right; white-space:nowrap; padding:3px 0;">{_fmt_dinero(subtotal)}</td>\n          </tr>\n        """\n    telefono = f"<br>Tel: {_escapar_html(cotizacion[\'cliente_telefono\'])}" if cotizacion.get("cliente_telefono") else ""\n    return f"""<!DOCTYPE html>\n<html><head><meta charset="utf-8"><title>Cotizacion {_escapar_html(cotizacion[\'folio\'])}</title><style>\n  body {{ width:380px; margin:0; padding:8px; font-family:monospace; font-size:13px; color:#000; }}\n  h1 {{ font-size:16px; text-align:center; margin:4px 0; letter-spacing:1px; }}\n  .centro {{ text-align:center; margin:2px 0; }}\n  .linea {{ border-top:1px dashed #000; margin:8px 0; }}\n  table {{ width:100%; border-collapse:collapse; }}\n  .total td {{ font-size:15px; font-weight:bold; padding-top:6px; }}\n</style></head><body>\n  <h1>MARK - INC</h1>\n  <p class="centro">Cotizacion {_escapar_html(cotizacion[\'folio\'])}</p>\n  <div class="linea"></div>\n  <p><b>Cliente:</b> {_escapar_html(cotizacion[\'cliente_nombre\'])}{telefono}</p>\n  <div class="linea"></div>\n  <table>{filas}</table>\n  <div class="linea"></div>\n  <table><tr class="total"><td>TOTAL</td><td style="text-align:right;">{_fmt_dinero(total)}</td></tr></table>\n  <div class="linea"></div>\n  <p class="centro" style="font-size:10px;">Cotizacion informativa, sujeta a cambios.<br>Vigencia 15 dias.</p>\n</body></html>"""\n'

# Este bloque aparece DOS veces identico en frontend/index.html.
HTML_DOBLE_VIEJO = '    notas: COT_ACTUAL.notas || null,\n    items: COT_ACTUAL.items.map(it => ({\n      articulo_id: it.articulo_id || null, clave: it.clave || null, nombre: it.nombre,\n      cantidad: Number(it.cantidad) || 0, precio_unitario: Number(it.precio_unitario) || 0,\n      descuento_pct: Number(it.descuento_pct) || 0, nota: it.nota || null,\n    })),'
HTML_DOBLE_NUEVO = "    notas: COT_ACTUAL.notas || null,\n    tipo_cliente: COT_ACTUAL.tipo_cliente || 'publico',\n    items: COT_ACTUAL.items.map(it => ({\n      articulo_id: it.articulo_id || null, clave: it.clave || null, nombre: it.nombre,\n      cantidad: Number(it.cantidad) || 0, precio_unitario: Number(it.precio_unitario) || 0,\n      descuento_pct: Number(it.descuento_pct) || 0, nota: it.nota || null,\n    })),"


def leer(ruta):
    with open(ruta, "r", encoding="utf-8", newline=None) as f:
        return f.read()


def escribir(ruta, contenido):
    with open(ruta, "w", encoding="utf-8", newline="") as f:
        f.write(contenido)


def aplicar_reemplazos(ruta):
    reemplazos = ARCHIVOS[ruta]
    try:
        contenido = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    cambios = 0
    hubo_error = False
    for viejo, nuevo in reemplazos:
        if nuevo in contenido:
            continue
        if viejo not in contenido:
            print(f"[{ruta}] No encontre un bloque esperado (el archivo pudo haber cambiado). Avisale a Claude.")
            hubo_error = True
            continue
        contenido = contenido.replace(viejo, nuevo, 1)
        cambios += 1

    if ruta == "frontend/index.html":
        ocurrencias = contenido.count(HTML_DOBLE_VIEJO)
        if ocurrencias > 0:
            contenido = contenido.replace(HTML_DOBLE_VIEJO, HTML_DOBLE_NUEVO)
            cambios += ocurrencias
        elif contenido.count(HTML_DOBLE_NUEVO) < 2:
            print(f"[{ruta}] No encontre las 2 copias esperadas del payload de guardado. Avisale a Claude.")
            hubo_error = True

    escribir(ruta, contenido)
    print(f"[{ruta}] {cambios} cambio(s) aplicado(s).")
    return not hubo_error


def aplicar_pdfs_cotizaciones():
    ruta = "backend/pdfs_cotizaciones.py"
    try:
        actual = leer(ruta)
    except FileNotFoundError:
        print(f"[{ruta}] No encontre el archivo -- corre esto desde la carpeta del repo.")
        return False
    if actual.strip() == PDFS_COTIZACIONES_COMPLETO.strip():
        print(f"[{ruta}] Ya esta correcto. No hace falta nada.")
        return True
    escribir(ruta, PDFS_COTIZACIONES_COMPLETO)
    print(f"[{ruta}] Actualizado completo (titulo segun tipo de cliente).")
    return True


def main():
    ok = True
    ok = aplicar_pdfs_cotizaciones() and ok
    ok = aplicar_reemplazos("backend/db.py") and ok
    ok = aplicar_reemplazos("backend/app.py") and ok
    ok = aplicar_reemplazos("frontend/index.html") and ok

    if not ok:
        print()
        print("Algo no se pudo aplicar automaticamente. Avisale a Claude que mensaje salio, sin correr git add/commit todavia.")
        sys.exit(1)

    print()
    print("Todo listo. Ahora corre:")
    print("   git add backend/app.py backend/db.py backend/pdfs_cotizaciones.py frontend/index.html")
    print('   git commit -m "Cotizador: tipo de cliente, descuento mas grande, flujo por pasos"')
    print("   git push")


if __name__ == "__main__":
    main()
